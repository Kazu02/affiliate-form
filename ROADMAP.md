# Roadmap

## Completed

- フォーム受信・回答記録・LINE通知の基本フロー
- 代理店SS分離・代理店コードによるフォーム振り分け
- 管理シート（フォーム一覧・回答数・最終回答日時）
- 自社フォームの紹介者フィールドをselectに自動変換
- 30件クエスト（4案件×30件、2026/05末）の進捗報告
- 日次レポート（前日集計のLINE通知、毎朝8時）
- 回答ヘッダー同期機能（fixAnswerHeaders）
- 顧客管理シートの自動作成・既存データインポート
- 150件クエスト実装（全自社フォーム合計、2026/06末、3名50件ノルマ）
- 広告主成果管理シートへのリアルタイム書き込み
- 広告主シートへの既存データ一括インポート（202604・202605・202606）
- 広告主シートの列構成確定（A=受信日時, B=広告名, C=お名前, D=紹介者名, E=スクショURL, F=トラッキング漏れ, G=承認区分）
- 承認区分（アフィリエイト管理の⭕/○系）をG列、トラッキング漏れ（❌/✖/×系）をF列チェックボックスにインポート
- 2026-06-29に広告主成果管理SSをバックアップ後、202604/202605/202606を再インポート（合計341件、承認139件、トラッキング漏れ57件、無視145件）
- 月別シート自動作成（createAdvertiserMonthSheet。テンプレートは最新の月シート）
- 広告主シートを月指定で再生成する経路（メニュー／`adv_admin` ルート）。過去月を巻き込まない
- 2026-07-30: 202607 を6月コピーから7月の実データ158件に再生成、202608 を空シートとして先行作成
- 2026-07-30: 202606 をメインSSと再同期（承認 42→88・トラッキング漏れ 7→39）。202604〜202608 の全月がメインSSと一致
- 広告主シートが無い月に成果が落ちる問題の恒久対策（毎月25日の `ensureAdvertiserMonthSheets` トリガー＋書き込み時の自動作成）
- 2026-08-05: 誤アカウント再デプロイで2日間止まっていた申請フォームを復旧（版118・オーナー再デプロイ・匿名200/CORS確認）
- 2026-08-05: public リポジトリから読めた保守ルートのキーを無効化（一時デプロイ@112/@115削除・`ADVERTISER_ADMIN_KEY`のScriptProperties化・`quest_admin`撤去）
- アフィリエイト報酬 振込先フォーム新設（独立GASプロジェクト `payout-form/`）。回答→統合顧客管理SS(SS1)のマスター＋総合_<担当>タブの `持ち家かどうか` 直後に自動追記。フルネームあいまい照合・先頭ゼロ保持・未照合ログ。E2E検証済み・本番稼働。
- 2026-08-19: 全31案件のアフィリンクを実際に開いて稼働13/停止18に仕分け。判定はASPの「このキャンペーンは終了しました」固定ページ（HTTPは常に200なのでコードでは判定不可）
- 2026-08-19: 稼働ボタン（案件マスタのチェックボックス）・1案件1タブ化・代理店リンク集の自動発行を実装し本番反映（@119）。稼働8件／非稼働13件＋旧代理店タブ10枚を非表示、全31シートに「代理店」列、停止中案件はフォームを出さない
- 2026-08-19: 代理店への稼働変更通知をメールで自動化（日次で稼働案件の集合を突き合わせ、変わったときだけ送信。初回は送らず基準を記録）。代理店の削除もメニュー化（顧客管理の担当者タブが空なら一緒に片付ける）
- 2026-08-19: 代理店別取扱案件（チェックで選択）・リンク集の実績表示・申請状況一覧（自社＋全代理店を色分け）・代理店への新規申請通知を実装し本番反映（@120/@121）
- 2026-08-20: 停止18件は終了で確定（貼り替えではない）／代理店名は小中様で確定／代理店別サマリー（件数・承認率・直近30日）を実装し、日次で申請状況一覧と一緒に再生成
- 2026-08-20: ASP（FPR）の管理画面を調査。獲得ログのCSV取得手順を確認し、クリック日時での突合精度を実測（48%・一致時は中央値0秒）。ASP承認なのに自社がトラッキング漏れ扱いの8件・77,000円を発見

## Current

- 広告主提出用シートの承認区分・トラッキング漏れ反映は本番デプロイと再インポートまで完了。
- 営業名簿を `JISHA_REFERRER_OPTIONS`（本体）/ `SALESPEOPLE`（振込先）に集約し `江口裕人` を追加（計9名）。本番反映済み（本体 version 99・紹介者選択肢14フォーム更新・顧客管理SS/SS2に担当タブ・振込先フォーム選択肢9択、いずれも江口裕人含む確認）。2026-07-16。
- 営業担当別 案件ステータス表（`buildSalesRepStatusSheets()`/`buildIntegratedRepSheets()`）は Codex 実装済・本番デプロイ済。事後コードレビュー（`CLAUDE_REVIEW.md`）を 2026-07-16 に実施し VERDICT: APPROVED。**残タスクだった「江口裕人 追加後の再生成」は 2026-07-24 に完了**（SS1 `総合_江口裕人` = 32行）。あわせて再生成が手動忘れになる構造自体を解消済み（下記）。
- **メンバー増減は「名簿を編集 → メニュー『営業担当を同期』を1回」で完結する**（2026-07-24）。`syncSalesRoster()` が案件列の整列と SS2/SS1 の再生成まで通しで行う。再生成2関数と案件列同期もメニューから単体実行できる。SS2 担当タブの案件列がバラバラで実績が黙って捨てられていた問題（`unmatchedCase`）も解消し、全9タブで案件列集合が一致。
- **藤井勇大 を名簿に追加（計10名）**（2026-07-24）。改修後の新手順を初めて通しで適用し、名簿編集→`clasp push`→`syncSalesRoster()` 1回で全系統が揃うことを実証。SS2/SS1 とも `藤井勇大` = 1行、`excludedReferrers` は `{}` に解消。振込先フォームの選択肢も10名に同期済（実物HTMLで確認）。フォーム顧客管理は @19 に再デプロイ。
- 補足: Codex 自走経路の実在を検証済（2026-07-16）。orchestrator は planning/review=`claude.exe`・implementation=実 `codex.js exec`(gpt-5.6-sol) で分離、Codex 失敗時に Claude が代行するフォールバックは無い（失敗＝run全体failed）。スモークテストで実Codexが `OK`/exit0 を返すことを確認。

- **広告主成果管理シートの月次運用が自動化された**（2026-07-30）。202607=158件（承認40/トラッキング漏れ13）、202608=空シート作成済。以降の月は毎月25日のトリガーが当月・翌月シートを用意するので手作業は不要。過去月の乖離はメインSSを正として反映する方針で確定し、202606 を再同期して全月一致させた（バックアップ取得済）。

- **2026-08-05 定期確認: 承認区分・トラッキング漏れは全月一致（ズレ0）。** `action=preview` で 202604〜202608 を突合し、行数・承認・トラッキング漏れが広告主シートとメインSSで完全一致することを確認（202604 18/6/1・202605 163/91/49・202606 171/88/39・202607 179/40/13・202608 12/0/0）。再生成は不要で、提出できる状態。7/30以降 承認の増加が0件で202607の承認率が22%と低い点はユーザー確認済みで**問題なし**（承認が付くペースの問題であって記入漏れではない）。以後この観点で追加調査はしない。

- **アフィリンクの生死で案件を仕分けた（2026-08-19）。稼働13件・停止18件。** 7月の請求対象6案件のうち
  **不動産一括査定イエイ（7月承認24件＝最多・最終回答8/01）と HOME4U不動産 が停止**しており、
  HOME4U系は4案件すべて停止。稼働側で件数が出ているのは 保険マンモス・リビンマッチ・いえカツLIFE・
  RE-Guide・ノムコム。**おうちクラベルは自社リンクのみ停止で小中様（代理店）リンクは稼働**＝広告主は
  出稿継続中で、無効なのは自社URLだけ。詳細と判定手順は DECISIONS.md 2026-08-19。

- **代理店運用の土台が本番に入った（2026-08-19・@119）。** 案件マスタの「稼働」チェックボックスが
  唯一の入力で、押した瞬間に設定タブの表示/非表示・代理店リンク集の掲載・申請フォームの受付が
  同時に切り替わる。代理店ごとのタブ複製は廃止し、識別は回答行の「代理店」列で行う。
  代理店を登録すると専用リンク集URLを発行してメールで送る（`agency.html` → `links.html`）。
  リンク集は開くたびにGASへ問い合わせるので稼働状況がリアルタイムに反映される。
  詳細と実装上の注意は DECISIONS.md 2026-08-19。

## Next

- **【要ユーザー・優先】ASP=承認なのに自社シートで「トラッキング漏れ（❌）」にしている8件（報酬合計77,000円）を確認する。**
  差0〜2秒・案件名一致で同一成果とみて間違いない。請求対象から外れているので、正しければ承認へ直す。
  対象月は 2026-06〜07。直したら広告主シートの該当月を再生成する。
- **突合を100%にするには CL付加情報に自社の申請IDを流す。** 獲得ログに CL付加情報1〜5 の欄があり
  現在は全件空。**クエリパラメータ名は広告のリンクタグを1件表示すれば分かる**（推測で決め打ちしない）。
  名前が分かればフォーム側はアフィリエイトURLへ `&<パラメータ名>=<申請ID>` を足すだけ。
- 獲得ログCSVの定期取得は、ログインがセッションCookieのため**GASからは叩けない**。PC上の
  ブラウザ自動化か、月1回の手動ダウンロード＋シート取り込みが現実的。まずは手動取り込みで足りる。
- **【将来】代理店通知のLINE化は「メールだと気づかれない」が起きてから着手する。** 統合アプリの
  既存 agent 役割に乗せる方針（案A）で確定済み。着手時の前提2つは DECISIONS.md 2026-08-19 に記録:
  紐づけは推測可能な代理店コードではなくトークンで行うこと、GASは X-Line-Signature を検証できないため
  統合アプリ経由の署名付きブリッジを使うこと。**アフィリンク単独では完結しない**（アプリ化にも手が入る）。
- **【要ユーザー】代理店登録フォームのURLは合言葉つき。** `setupCaseAgencyFeature()` の実行ログに
  出た `agency.html?k=...` を代理店へ渡す。**合言葉はソース・Git・ボードへ書かない**
  （ScriptProperties の `AGENCY_SIGNUP_KEY` が正）。無効化したいときはプロパティを消せば
  fail-closed で誰も通らなくなる。
- **稼働の切り替えは案件マスタのチェックボックスだけで完結する。** 新しい案件を作ったら
  メニュー「案件マスタを同期＋稼働を反映」を1回実行して行を増やす（既定は非稼働＝非表示）。
- スマモニ・ポケットリサーチはリンクが生きているが非稼働として設定済み。稼働させるなら
  案件マスタのチェックを入れるだけでよい。
- **【要ユーザー】停止18件のうち直近まで成果が出ていた案件のURLを確認する。** ASPの文言は
  「掲載が終了しているか、URLに誤りがあります」で**終了とリンク貼り替えを区別しない**。
  優先度順に 不動産一括査定イエイ / HOME4U不動産 / HOME4U家づくりのとびら / 出会えるエージェント。
  新URLを受け取ったらメインSSの該当 `設定_*` タブの「アフィリエイトURL」を差し替えるだけでよい
  （フォーム側・GAS側の変更は不要）。**代理店（小中様）のリンクを自社へ流用しない**（成果が代理店に付く）。
- 稼働状況を継続的に見るなら、`設定_*` の全URLを叩いて `稼働状況` シートへ書き出す定期処理を足す案がある。
  **`管理` シートに列を足すのは不可**（`updateManagementSheet()` が `clearContents()` で6列に作り直すため消える）。
- **【実装済み 2026-08-19】代理店が増えてもURL管理が破綻しない構造にする（設計メモ）。**
  現在は `設定_<案件×代理店>` タブ1枚がフォーム定義とアフィリエイトURLを兼ねているため、
  代理店が増えるとタブ数が 案件21×代理店M で増え、URLの所在が散る（M=5 で約105タブ）。
  実際いま31本中18本が死んでおり、フォームは死んだリンクでもボタンをそのまま出す。
  - **(1) `リンク台帳` シートを作り、URLの編集場所をそこ1か所にする。** 1行=1(案件×代理店)で
    案件コード/案件名/代理店コード/代理店名/フォーム記号/URL/状態/最終確認/最終到達先/備考。
    `readConfig` は「台帳に行があれば台帳のURL、無ければ従来どおり設定タブ」とし、記入漏れで
    壊れないようにする（二重管理も起きない）。初期31行は既存タブから自動生成できる。
  - **(2) 死活チェックを `checkAffiliateLinks()` として自動化し、稼働→停止に変わったときだけ
    LINE通知する。** 判定は2026-08-19 の DECISIONS.md の方法（最終到達ホスト＋「このキャンペーンは
    終了しました」の文言。meta refresh と location.href の中継を追う）。
    **⚠️ チェック1回につきASPのクリックが1回計上される**ため毎日は回さない。週1回とし、
    最終回答日時が直近のものはチェックを飛ばして打鍵数を減らす。
  - **(3) 状態=停止の案件はフォームにリンクボタンを出さない。** いま停止中でもボタンが出るので、
    顧客は「このキャンペーンは終了しました」に着地して申請できずに終わる。営業の信用に直接響くので
    ここが一番効く。doGet の config に停止フラグを載せ、フォーム側は案内文へ差し替える。
  - **(4) 命名規則**: 新規フォーム記号は `<案件コード>_<代理店コード>`。**区切りは `_` 固定**
    （`FORM_CODE_PATTERN` が `[a-zA-Z0-9_]+` でハイフンを弾き、`-s`/`-m`/`-i`/`-e` は紹介者固定リンクの
    予約語のため）。**既存の記号（`ouchikuraberukn` 等）は変えない**——`?form=<記号>-s` の固定リンクを
    営業へ配布済みで、変えると全部死ぬ。
  - **(5) 代理店追加の手順が「台帳にN行貼る→メニュー1回」に収束する**のが最終形。
  - **やらないこと**: フォーム定義（タイトル・説明文・項目）の重複解消は今回やらない。項目が
    お名前・紹介者名の2つしかなく実害が小さいのに対し、回答データが同居する設定タブの構造を
    触ることになりリスクに見合わない。項目が増えてきたら案件マスタとして切り出す。
  - 台帳の実物（31行・今日の死活結果入り）は `02_設計/リンク台帳_案_20260819.csv`
    （**public リポジトリのため gitignore 済み**。bIdが並ぶので公開側へ出さない）。
- **保守ルートを再開したくなったら**: `adv_admin` は残っているが `ScriptProperties` の `ADVERTISER_ADMIN_KEY` が未設定のあいだは誰も通さない（fail-closed）。使うならGASエディタの「プロジェクトの設定 > スクリプト プロパティ」で**新しい値**を設定する（露出した旧値は使わない）。設定しなくても広告主シートの確認・再生成はスプレッドシートのメニューから実行できる。
- **【残・構造対応】GASソースとPages配信物のリポジトリ分離。** このリポジトリが public なのは GitHub Pages の配信元だから。キーは除去済みで急ぎの穴は無いが、GASソースと内部ドキュメントが公開側に同居する構造は残っている。分けるならGAS側を別の private リポジトリへ移す。
- **【残】git履歴に旧キーが残る。** 現行HEADからは除去済みだが履歴には残る。public リポジトリなので取得済みの前提で考え、旧値を再利用しないことで無効化している。履歴の書き換えは行わない方針（push済み履歴を書き換えない）。
- 広告主へ提出する前に `action=preview`（またはスプレッドシートのメニュー）で広告主シートとメインSSの差分を確認し、ずれていれば対象月を再生成する（承認は後から付くのでリアルタイム書き込みには乗らない）。

### 2026-08-19 アフィリエイト代理店の登録・承認まわり自動化の打ち合わせ で決まったこと

- ASPのログインIDとパスワードを sugawa kazuki に送る（担当: すがわらたかひろ / 期限: この日のうち）
- 共有されたASPの管理画面を確認し、承認の突合を自動化できるか判断して回答する。自動化が難しければ手動運用にするか、もっと見やすい方法を考えて提示する（担当: sugawa kazuki）
- 現在稼働中の案件をまとめて sugawa kazuki に共有する（担当: すがわらたかひろ）
- アフィリエイト管理スプレッドシートに案件の稼働中チェック欄を追加し、稼働中の案件だけが表示されるようにする（担当: sugawa kazuki）
- 稼働中の案件数ぶんのアフィリエイトリンクを、代理店追加ボタン一回でまとめて生成できるようにする（担当: sugawa kazuki）
- 小永（コナカ）さんの案件を非稼働にして一覧から非表示にする（担当: sugawa kazuki）
- 代理店登録フォームを用意し、送信完了で登録メールアドレスへ稼働中案件のアフィリエイトリンク生成ページが届くようにする（担当: sugawa kazuki）
- 承認状況の一覧を全代理店の一元表示に変更し、代理店ごとに色分けする（担当: sugawa kazuki）
- アフィリエイト代理店をやる人（見込み3〜4人）に代理店登録をしてもらう（担当: すがわらたかひろ）
- ウォーターサーバー・電気の登録用スプレッドシートの完成を待ち、完成後に代理店用を含む市場作りの管理用の仕組みを作る（担当: sugawa kazuki / 期限: スプレッドシート完成後）
- 勉強会の題材として「人に任せたい仕事・自分がやりたくない仕事・自分じゃないといけないが面倒な仕事」をメンバーから聞き取ってメモしてもらう（アポと並行して収集する）（担当: すがわらたかひろ）

## Blockers

- なし（2026-08-05 にフォーム停止を解消）。

## Backlog

- 広告主シートへのリアルタイム書き込み: 現在は承認=false固定。承認は後から付くため広告主シートは放置すると必ずメインSSとずれる。当面は提出前に `action=preview` → 必要なら月指定で再生成、という運用で吸収する（自動追従は未実装）。
- 顧客管理シートの電話番号・顧客ID列は将来追加予定（ユーザー言及あり）。
- 150件クエストは2026/06末で終了予定。次クエストの定義は未定。
