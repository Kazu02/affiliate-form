# 実装レポート — 営業担当別 案件ステータス表（{月}月{状態}）

> 注: Codex(orchestrator)の認証失効(`refresh_token_reused`)で自動実装が起動できなかったため、**Claudeが実装・実行**した（ユーザー承認済み・方針B）。

## 変更ファイル
- `gas-project/Code.gs`: 末尾に `buildSalesRepStatusSheets()` と `repStatusRepAliasMap_()` を**純粋追加**（`doPost`/`doGet`/既存関数は不変更）。
- `gas-project/.gitattributes`: 新規（`*.md -whitespace`）。

## 実装内容
- 正データ = C（アフィリエイト管理SS、`getOrCreateSpreadsheet()`＋IDガード `1JOMT…`）の `設定_` 自社フォームタブ（代理店/小中様は `agencyCode!==house` で除外）。
- 各申請行から `お名前`/`紹介者名`/`受信日時`/`承認`（ヘッダー名で特定）を読み、
  状態=`getAdvertiserApprovalFlags`（⭕系→承認／❌系→未承認／△・空・他→申請）、月=`toJSTDateStr`の月。
- 担当=`紹介者名`を8名に正規化（`normalizeName`＋別名表、柳澤→柳沢等）。同一顧客×案件は受信日時最新を採用。
- 出力 = SS2（`1TARRQ…`）の8担当タブのみ、ヘッダー以下 `clearContent`→再書込。セル=`{月}月{状態}`。電話/顧客ID空。

## 実行方法（clasp run 不可のため一時Webアクション方式）
1. `doGet` にガード付き一時アクション（`?action=buildRepStatus&key=<一時鍵>`）を追加 → `clasp push` → `clasp deploy`（版77）。
2. 一時URLをcurlで実行（executeAs=USER_DEPLOYING=shinhogle）。
3. **一時アクションを撤去** → `clasp push` → `clasp deploy`（版78）で原状復帰。Code.gsに一時鍵は残さず（検証済み=0件）。
- アカウント: `clasp show-authorized-user`=shinhogle@gmail.com を確認。`clasp push` 前にリモート本番=ローカル追記前と一致を確認（末尾追加のみ・削除事故なし）。

## 検証結果（実行ログ）
- 対象 `設定_` タブ=14。**案件突合の取りこぼし 0（unmatchedCase 空）**。月不明 0。
- 担当別行数: 岩本100 / 菅原44 / 村井16 / 柳沢9 / 藤森2 / 大島0・小椋0・細川0。
- 最終セル状態別: 承認183 / 申請82 / 未承認86。
- SS2実データ確認: 柳沢タブ等で `{月}月{状態}`（例「4月承認」「5月未承認」「5月申請」）を確認。他タブ・他ブック不変。

## 未解決・申し送り
- **除外4件**: 紹介者名が「松田恵美（岩本拓也）」「池田千鶴(岩本拓也)」「オクノ(岩本拓也)」の**括弧内に担当**パターン。8名に正規化できず除外。
  → 「2名記載＝アフィリンク優先」の一種として、括弧内の担当（岩本拓也）に割り当てる拡張を検討可（要ユーザー判断）。
- `buildSalesRepStatusSheets()` は恒久関数として残置。今後は Apps Script エディタから Run で SS2 を再生成できる。
- 本番Webアプリ挙動は不変（doGet/doPost 純粋不変・deploy @78 は一時アクション撤去後）。

## 追記（2026-07-06）: 括弧内担当の救済＋原状復帰
- `resolveRepCanonical_()` を追加し、紹介者名「顧客名（担当）」の**括弧内の担当名も8名に割り当て**。
- 再実行結果: **excludedReferrers=0（4件解消）**、岩本拓也 100→**102**、申請84・未承認88（承認183不変）、案件突合0。
- 一時アクション撤去 → deploy **@80** で原状復帰（一時鍵の残存=0を確認）。

## 追記2（2026-07-06）: 未承認→非承認 リネーム ＋ 統合顧客管理(SS1)の担当別分割
- **表記変更**: 状態ラベル `未承認`→`非承認`（`buildSalesRepStatusSheets` 内6箇所）。SS2再生成で反映を確認（岩本タブに「非承認」出現・「未承認」残存0）。
- **新規関数** `buildIntegratedRepSheets()`（純粋追加）: 統合顧客管理ブック（SS1 `1aaiCIDQ…`）の「統合顧客管理」タブを営業担当別に分割し、SS1内に **`総合_<担当>` タブ**を生成。
  - 全27列（顧客ID/名前/…/アフィリンク顧客名/状態）を保持。案件（商材）列は C の申請状態がある顧客のみ **`{月}月{状態}`** に置換（無ければ統合の元値を維持）。
  - 担当割当は**アフィリンク優先**（Cの紹介者→無ければ統合の営業担当）。8名以外の担当（例: 須川一輝）は**生値でタブ化**し取りこぼしゼロ。営業担当も紹介者も空の行のみ除外。
  - 色分け: 承認=`#d9ead3` / 非承認=`#f4cccc` / 申請=`#fff2cc`。入力規則は各タブ `clearDataValidations()` で除去。
- **実行結果**（一時アクション `buildRepStatus` で両関数同時実行）:
  - SS2: 承認183 / 申請85 / 非承認88、excludedReferrers=0、unmatchedCase=0。
  - SS1: 総合_柳沢12 / 岩本123 / 菅原43 / 村井15 / 大島0 / 小椋0 / 細川0 / 藤森2 / **須川一輝3**、excluded=0。
  - SS1実データ確認: `総合_村井亮介` 先頭行「馬越奈緒美: 6月非承認/5月非承認/5月承認/5月承認/5月非承認」等、`{月}月{状態}` 置換を確認。全9タブ生成をxlsx workbook.xmlで確認。
- **原状復帰**: 一時アクション撤去 → push → deploy **@87**（一時鍵残存0を確認、`?action=buildRepStatus` は通常フォーム設定を返すのみ＝無効化済み）。本番Webアプリ（doGet/doPost）不変。

## 追記3（2026-07-06）: 枠線追加 ＋ 幽霊行のC照合削除
- **枠線**: SS2の8タブ・SS1の総合9タブに `setBorder(true×6)` で格子線を追加（xlsxで thin border 確認）。deploy @89。
- **調査（柳沢ページの申請済のみ行）**: C（設定_フォーム群）を真データとして統合顧客管理の「アフィリンクのみ」124行を全照合（氏名fold＋担当）。
  - 結果: OK=121 / 問題=3行。
    - 高橋祐樹(柳沢) … Cは**髙橋祐樹＝菅原**(異体字高⇔髙・担当違い)。正行は別に存在。
    - 榎本(柳沢) … Cは**榎本彩人＝岩本**(部分名)。正行は統合済で別に存在。
    - 岩鼻ひかる(菅原) … **Cに一切なし**（純粋な幽霊行）。
  - いずれも状態=アフィリンクのみ＝他CRM情報の損失なし。
- **新規関数** `cleanupIntegratedPhantomRows()`（純粋追加）: 上記3件を氏名＋担当＋状態で内容一致した行のみ削除（各条件一致1件でなければSKIP／削除候補>8で中止／削除前スナップショットをログ化）。
- **重要な原因（GAS落とし穴）**: 統合顧客管理に**フィルタ**が設定されていると `deleteRow` が**例外を出さず無言で失敗**する（`insertRowsAfter` は成功するため権限/保護ではないと切り分け）。対策として cleanup 内で **フィルタ除去→行削除→データ範囲へフィルタ再作成** を実装。
- **実行結果**: 統合顧客管理 lastRow 199→**196**（3行削除確定）、髙橋祐樹(菅原・正)は残存。総合再生成で 柳沢 12→**10** / 菅原 43→**42**。excluded=0。
- **原状復帰**: 全一時/診断アクション（cleanupPhantom/diag2/diag3/diagIntegrated）撤去 → deploy **@96**（一時鍵残存0、無効化確認済み）。恒久関数は残置しエディタから再実行可。本番Webアプリ不変。
