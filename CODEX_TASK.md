# Codex タスク指示書 — 営業担当別 案件ステータス表（{月}月{状態}）を SS2 に生成

## 目的
アフィリエイト申請の「顧客×案件」ステータスを、**営業担当（紹介者）ごとのタブ**へ書き出す GAS 関数
`buildSalesRepStatusSheets()` を `gas-project/Code.gs` に**新規追加**する。各案件セルは `{申請月}月{状態}`
（例「5月承認」「6月申請」「5月未承認」）。出力先は既存の営業担当別ブック（SS2）の**8タブのみ**を
非破壊で書き換える（対象8タブのみ ヘッダー以下 clearContent→再書き込み。他タブ・他ブックは読み取りのみ／不変更）。

このフェーズは**実装のみ**。`clasp push`／`deploy`／関数実行／本番反映は**行わない**（後段でゲート付き・手動実施）。
`doPost`／`doGet`／既存関数は**不変更**（純粋な追加関数）。

## 対象スプレッドシート
- **C（読み取り・正データ）= アフィリエイト管理SS（メインSS）**
  - アクセスは**必ず既存アクセサ `getOrCreateSpreadsheet()` を使う**（ScriptProperties `SPREADSHEET_ID` を参照する既存関数）。
  - **ガード**: `getOrCreateSpreadsheet().getId()` が `1JOMT_Uuoq3H6O9lZcAKuSpzC2ehUJ35k53mVvnZRwY0` と**一致することを assert**。
    不一致なら**処理を中止**し、実 ID と名前をログに出す（誤ブックへの書き込み事故を防ぐ。ID をハードコードで `openById` しない）。
  - 各 `設定_<...>` タブに申請1件＝1行。回答データは **G列（`ANSWER_START_COL = 7`）以降**、**1行目がヘッダー、2行目以降がデータ**。
  - 列は**ヘッダー名で特定**（列位置固定禁止）: `受信日時` / `お名前`（顧客名）/ `紹介者名`（営業担当）/ `承認`（記号）。
- **SS2（書き込み）= 営業担当別ブック**: `1TARRQ_hqnptRGeEu5WzH1hlVZ2jqlfGunsHRcxbTWsg`（この ID は既存アクセサが無いため `openById` で直接開いてよい）
  - 既存タブ（＝営業担当名、これ**以外は作らない・触らない**）: 柳沢悠貴 / 岩本拓也 / 菅原貴博 / 村井亮介 / 大島雅史 / 小椋裕也 / 細川貴弘 / 藤森宣哉。
  - **8タブが実在することを起動時に確認**。欠けているタブがあればログに出し、その担当はスキップ（新規作成しない）。
- ※SS1（統合顧客管理）は**使わない**（統合セルは「申請済」のみで承認・月を持たないため、正データ C から直接組み立てる）。

## 対象タブの選別（C 側）— 既存の実証済みパターンを流用
`importExistingToAdvertiserSheetCore_`（`Code.gs:2441` 付近）と**同じ除外ロジック**を使うこと:
1. `sheet.getName().startsWith(CONFIG_PREFIX)`（`設定_`）で始まるタブのみ対象。
   → これにより `管理` / `グループIDログ` / `回答一覧` 等の非 `設定_` タブは自動除外。
2. 各タブのデータから `AGENCY_KEY`（`代理店コード`）行を読み、`agencyCode !== AGENCY_DEFAULT`（`house`）なら**除外**。
   → これが**代理店版（`（小中様）` 等）を除外**する正しい手段。タブ名の文字列一致に依存しない。
3. 残ったタブが本タスクの集計対象（自社フォーム）。

## 案件列（SS2ヘッダー）↔ C の `設定_` タブ の対応 — 表示名で突合
- SS2 の**ヘッダー行（1行目）は現行を維持**（列順固定・改変禁止）:
  `顧客名 | 電話番号 | 顧客ID | リビンマッチ | 不動産一括査定イエイ | おうちクラベル | HOME4U不動産 | HOME4U土地活用 | SREリアリティー | キレイライン | みんなの生命保険アドバイザーオンライン | みんなの生命保険アドバイザー対面 | アイネット証券 | スマモニ | ポケットリサーチ | 保険マンモス | HOME4U家づくりのとびら`
  - `電話番号` / `顧客ID` 列は**空のまま**（現行踏襲）。
- **各 `設定_` タブの案件名は `getFormDisplayName(sheet, formCode)`（`Code.gs:598`）で解決**し、その表示名を SS2 ヘッダー文字列と突合して案件列を決める。
  **タブ名から `設定_` を除いた文字列で突合しない**（タブ名と表示名は一致保証がないため）。
- 突合は SS2 ヘッダーを実際に読み取り（列順・表記の差異に追従）、`indexOf` で列を決めること（列番号のハードコード禁止）。
- **突合できなかった場合の扱い（サイレントに捨てない）**:
  - `設定_` タブの表示名が SS2 ヘッダーのどれにも一致しない → その案件は書けない。**タブ名・表示名・件数をログ**に出す。
  - SS2 ヘッダーの案件列に対応する `設定_` タブが無い → その列は空のまま。

## 分類・整形ルール
1. **状態**（`承認` 列の記号 → 文字列。表記揺れ吸収は既存 `getAdvertiserApprovalFlags`（`Code.gs:2594`）の記号セットを流用）:
   - `approved` 判定（`⭕ ○ ◯ 〇` 系、FE0F 除去込み）→ **承認**
   - `trackingMissing` 判定（`❌ ❎ ✖ ✕ × ☓ ✗ ✘` 系）→ **未承認**
   - 上記いずれでもない（`△ ▲ 🔺`・**空欄**・その他）→ **申請**
   - ※本タスクは広告主インポートと違い、「その他/空欄」を**申請**として扱う（無視しない）。この差分を明記した上で `getAdvertiserApprovalFlags`
     の `{approved, trackingMissing}` を呼び、`approved→承認 / trackingMissing→未承認 / それ以外→申請` にマップすれば記号網羅を再利用できる。
2. **申請月** = その行の `受信日時` の月。**`toJSTDateStr(row[rtIdx])`（`Code.gs:1392`）で JST 日付文字列を得てから月を取り出す**
   （生 `Date.getMonth()` はタイムゾーンずれの恐れがあるため使わない）。`受信日時` が空で月が取れない行は**申請済みだが月不明**として集計から除外し、件数をログ。
3. **セル値** = `` `${月}月${状態}` ``（例 `5月承認` `6月申請` `5月未承認`）。申請の無い案件は**空セル**。
4. **営業担当（紹介者名）の正規化 → 8名の正規名**（この用途の単一ヘルパは既存に無い。以下を**明示実装**する。
   まず `normalizeName()`（`Code.gs:1418`、空白除去＋ひらがな化＋小文字化）を通し、その正規化キーで下表に当てる）:
   - `柳澤 / 柳沢 / 橋澤 / 橋沢` → `柳沢悠貴`
   - `岩本` → `岩本拓也` ／ `菅原` → `菅原貴博` ／ `村井` → `村井亮介`
   - `大島` → `大島雅史` ／ `小椋` → `小椋裕也` ／ `細川` → `細川貴弘` ／ `藤森` → `藤森宣哉`
   - 上記に当たらない紹介者名（例 `市場作り` 等）は**対象外**（どのタブにも書かない）。**除外した生の紹介者名と件数をログ**に出す。
   - 実装は「別名の正規化キー → 正規名」の対応表で行い、`normalizeName(別名)` をキーに引くこと（漢字表記ゆれを吸収するため）。
5. **顧客名の突合は不要**（C の `お名前` をそのまま顧客名として使う）。
   同一タブ（＝同一担当）内で **同じ顧客 × 同じ案件が複数申請**あった場合は **`受信日時` が最大（最新）の1件**を採用。
   顧客同一判定キーは `normalizeName(お名前)`（表示は元の `お名前` 文字列）。

## 出力（SS2 各タブ）
- データ行 = その担当が（正規化後に）紹介した顧客ごとに1行。各案件セルに `{月}月{状態}`、申請の無い案件は空。
- **書き込み前に、対象タブのヘッダー以下（2行目以降）を `clearContent` してから再書き込み**（`deleteRows` は使わない。既存の
  `importExistingToAdvertiserSheetCore_` と同方針。1行目ヘッダーは保持）。
- 書き込みは**対象8タブのみ**。8タブ以外・C・SS1 は不変更。
- `電話番号` / `顧客ID` 列は空のまま維持。

---

## 実装タスク（番号順・ファイル所有権・検証・依存関係）

> **並列実行の可否**: 本タスクの実装対象は単一ファイル `gas-project/Code.gs` に集約される。CLAUDE.md の並列ポリシー
> （同一ファイルを複数ワーカーが同時編集しない）に従い、**T1〜T3 は同一の実装者が順に行う**（ファイルレベルの安全な並列化は不可）。
> T4（レビュー）は実装者と分離し、T1〜T3 完了後に実施する。

### T1. 集計コア関数 `buildSalesRepStatusSheets()` の実装
- **所有ファイル**: `gas-project/Code.gs`（末尾に純粋追加。既存関数・`doPost`/`doGet` は不変更）
- **内容**:
  - C を `getOrCreateSpreadsheet()` で開き、ID ガード（上記）を実施。
  - 「対象タブの選別」ルールで `設定_` 自社フォームタブを列挙。
  - 各タブをヘッダー名で読み（`受信日時`/`お名前`/`紹介者名`/`承認`）、行ごとに
    状態（分類ルール1）・月（ルール2）・担当正規化（ルール4）・案件列＝`getFormDisplayName` 突合（案件列マッピング）を算出。
  - `担当 → { 顧客キー → { 案件表示名 → {月, 状態, 受信日時} } }` を組み立て、同一顧客×案件は最新（ルール5）で上書き。
  - **この T1 では SS2 への書き込みはまだ行わず**、集計結果と各種カウンタ（下記ログ項目）を返す構造にしておくと T2 が薄くなる（任意）。
- **依存**: なし（先頭タスク）
- **検証**: 構文が通ること（`clasp push` はしない。Apps Script エディタ保存 or ローカル lint で可）。関数が既存関数を上書き・改変していないこと。

### T2. SS2 への非破壊書き込み
- **所有ファイル**: `gas-project/Code.gs`（T1 と同一ファイル。**T1 完了後に着手**）
- **内容**:
  - SS2 を `openById("1TARRQ_hqnptRGeEu5WzH1hlVZ2jqlfGunsHRcxbTWsg")` で開き、8タブの実在確認。
  - 各担当タブのヘッダー（1行目）を読み、案件列 `indexOf` を確定（列ハードコード禁止）。
  - ヘッダー以下 `clearContent` → 集計結果を `{月}月{状態}` で書き込み。`顧客名` 列に顧客名、案件セルに状態、`電話番号`/`顧客ID` は空。
- **依存**: T1（集計結果を消費）
- **検証**: 対象8タブのみ変更。他タブ・他ブック不変。手動実行は T3 の手段で後段（このフェーズでは書き込み実行しない）。

### T3. 一時実行手段（ガード付き `doGet` アクション）と撤去手順
- **所有ファイル**: `gas-project/Code.gs`（T1/T2 と同一ファイル。**T2 完了後に着手**）
- **内容**:
  - `clasp run` は使用不可のため、Web 経由で実行するための**ガード付き一時アクション**（例 `?action=buildRepSheets` に
    簡易トークン等のガード）を `doGet` に足す想定手順を、**関数本体とは分けてコメントで明記**する。
  - **重要**: 実運用フェーズで足す／実行後に撤去する運用のため、**本コミットにはデフォルトで一時アクションを残さない**
    （＝ `doGet` の既存挙動を変えない）。撤去漏れがないよう、手順を `CODEX_REPORT.md` に必ず記載。
- **依存**: T2
- **検証**: 既定の `doGet` 挙動が現行と同一（差分が純粋追加関数のみ）であること。

### T4. レビュー準備（実装者→レビュアー分離）
- **所有ファイル**: `CODEX_REPORT.md`（新規／Codex が記載）。`CLAUDE_REVIEW.md` は Claude が別途記載。
- **内容**: 変更ファイル・実行方法（一時アクションの追加/撤去手順含む）・検証結果（件数・突合サンプル）・残リスクを `CODEX_REPORT.md` に記載。
- **依存**: T1〜T3
- **検証**: 下記「検証」節の全ログ項目とサンプル突合が揃っていること。

---

## 実行・検証（後段のゲート付き手動フェーズで実施。本フェーズでは実装まで）
- 実行はデプロイ手順（`PROJECT_CONTEXT.md` の必須2ステップ `clasp push` → `clasp deploy -i <Deploy ID>`）に従う。
  GASアカウントは **shinhogle@gmail.com**（`clasp login` で確認。誤アカウント厳禁）。**このフェーズでは実行しない**。
- **実行ログに必ず出す項目**:
  - C の ID ガード結果（実 ID／名前）。
  - 対象 `設定_` タブ数と、その表示名一覧。
  - 対象申請件数（総数）。
  - 8担当それぞれの出力行数。
  - 正規化できず除外した**生の紹介者名と件数**。
  - 各状態（承認／申請／未承認）の件数。
  - 受信日時が空で月不明のため除外した件数。
  - SS2 ヘッダーに突合できなかった `設定_` 表示名（あれば）と件数。
- **実データ検証**: SS2 各タブが `{月}月{状態}` 形式で埋まること。代表1〜2件を C の該当行と突き合わせ、担当・案件・月・状態が一致すること。
  他タブ・他ブックが変更されていないこと。

## 制約・安全
- **既存タブ（8担当タブ）のみ書換。C・SS1・SS2 の他タブは読み取り／不変更。**
- 既存関数・`doPost`/`doGet` を壊さない（差分は純粋な追加関数＋任意の小ヘルパのみ）。無関係な変更を混ぜない。
- このフェーズで **`clasp push`/`deploy`/関数実行/本番反映を行わない**。コミット・デプロイはオーケストレータのゲートで実施。
- 認証情報・個人情報・生の顧客データをメモリ／コード／Git に保存しない。

## 完了条件
- [ ] `buildSalesRepStatusSheets()` を `Code.gs` 末尾に追加（C の `設定_*`〈代理店/小中様は agencyCode で除外〉から集計、担当を8名に正規化、状態＋申請月、同一顧客×案件は最新採用）。
- [ ] 案件列は `getFormDisplayName` の表示名で SS2 ヘッダーに突合（タブ名スティップングを使わない）。突合不能はログ。
- [ ] 状態分類は `getAdvertiserApprovalFlags` の記号セットを流用し、その他/空欄は「申請」。月は `toJSTDateStr` から算出。
- [ ] SS2 の8タブを現行ヘッダー・列順のまま `clearContent`→再書き込み。`電話番号`/`顧客ID` は空。他タブ・他ブック不変。
- [ ] C は `getOrCreateSpreadsheet()` 経由＋ID ガード。SS2 は `1TARRQ...` を `openById`、8タブ実在確認。
- [ ] 一時実行手段は本コミットに残さず（`doGet` 既定挙動不変）、追加/撤去手順を `CODEX_REPORT.md` に明記。
- [ ] 検証ログ項目（上記全て）と実データ突合サンプルを `CODEX_REPORT.md` に記載。Claude が `CLAUDE_REVIEW.md` で確認して確定。

---

## 付録: 再利用する既存コード（`gas-project/Code.gs`）
| 用途 | 関数／定数 | 位置 |
|------|-----------|------|
| メインSS（C）取得 | `getOrCreateSpreadsheet()` | Code.gs:197 |
| `設定_` 判定 | `CONFIG_PREFIX`（=`設定_`） | Code.gs:6 |
| 代理店除外の判定 | `AGENCY_KEY`（`代理店コード`）／`AGENCY_DEFAULT`（`house`） | Code.gs:14,15 |
| 対象タブ列挙＋代理店除外の実例 | `importExistingToAdvertiserSheetCore_()` | Code.gs:2441 |
| フォーム記号取得 | `getFormCodeFromSheet(sheet)` | Code.gs:48 |
| 案件表示名の解決 | `getFormDisplayName(sheet, formCode)` | Code.gs:598 |
| 回答開始列 | `ANSWER_START_COL`（=7, G列） | Code.gs:（定数） |
| 承認記号→フラグ | `getAdvertiserApprovalFlags(value)` | Code.gs:2594 |
| 受信日時→JST日付文字列（月抽出用） | `toJSTDateStr(value)` | Code.gs:1392 |
| 氏名の正規化キー | `normalizeName(name)` | Code.gs:1418 |
| 8名の正規名一覧 | `JISHA_REFERRER_OPTIONS` | Code.gs:26 |
