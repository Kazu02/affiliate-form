# Decisions

## 2026-06-12: Adopt File-Based Project Memory

- Decision: Store durable project context in `PROJECT_CONTEXT.md`, `DECISIONS.md`, and `ROADMAP.md`.
- Reason: Claude and Codex sessions do not reliably share complete conversation history.
- Consequence: Important conclusions from project chats must be summarized into these files.
- Safety: Do not copy credentials, personal data, confidential raw data, or full chat transcripts.

---

## 2026-07-30: 過去月の乖離はメインSSを正として反映（202606を再同期）

- Decision: 広告主シートとメインSS（正データ）がずれている月は、**メインSSを正として広告主シートへ反映する**。ユーザー判断（2026-07-30）。
- Operation: バックアップ取得後に 202606 を再生成。171件は変わらず、承認 42→88、トラッキング漏れ 7→39 に是正。バックアップ = `【市場作り】成果管理シート バックアップ 20260730-2354 (202606再生成前)` (`1Laye0Uho0Cp0uhHLm0z3NNJH4ZdXnYwHV8D3u8z7qdM`)。
- 事前確認: 202606 の 備考(H列) は171行すべて空で、再生成で失われる手入力が無いことを確認したうえで実行した。
- 結果: 202604/202605/202606/202607/202608 の全月がメインSSと一致（件数・承認・トラッキング漏れ）。
- 乖離の原因: 6/29のインポート後に付いた承認がリアルタイム書き込みに乗らない（`writeToAdvertiserSheet` は承認=false固定）。**乖離は今後も毎月たまる**ので、広告主へ提出する前に `action=preview` で差分を見て必要なら再生成する運用にする。

- **注意（調査手法の教訓）**: gviz の `sheet=<名前>` は**名前を解決できないと黙って先頭シートの内容を返す**。これで 202604 を「3件しかない」と誤読した（実際は18件で正常）。広告主シートの件数を数えるときは gviz ではなく `action=preview`（A列の値で数える `countAdvertiserSheetRows_`）を使う。

---

## 2026-07-30: 広告主成果管理シートを月指定で再生成できるようにし、202607/202608 を整備

- Decision: 全月を一括で作り直す `importExistingToAdvertiserSheet` とは別に、**月を指定した月だけを再生成する**経路を追加した（`importAdvertiserMonthsCore_`／メニュー「広告主シートを月指定で再生成」／保守ルート `adv_admin`）。収集処理は `collectAdvertiserRowsByMonth_()` に切り出し、全月版と共用する。
- Reason: 202607 は 202606 をコピーしただけで中身が6月のままだった。全月インポートで直すと提出済みの過去月まで書き換わるため、対象月だけを触れる必要があった。
- Operation: 202607 を再生成（158件・承認40・トラッキング漏れ13）、202608 を空シートとして新規作成（タイトル行＋ヘッダー行のみ）。202604/202605/202606 は未変更。
- Consequence: 月次の作り直しが過去月を巻き込まなくなった。書き込み開始行は `ADVERTISER_DATA_START_ROW`(=3) で固定し、クリア直後の `getLastRow()` に依存しない。

---

## 2026-07-30: 広告主シートが無い月は成果が丸ごと落ちる問題を月次トリガーで塞いだ

- Decision: `ensureAdvertiserMonthSheets()`（当月・翌月のシートを先回り作成）を追加し、**毎月25日3時**の月次トリガーで走らせる。あわせて `writeToAdvertiserSheet` もシートが無ければその場で作るようにした。
- Reason: `writeToAdvertiserSheet` はシートが無い月をログだけ残して黙ってスキップする。実際 2026年7月は 202607 が存在しないまま月が進み、リアルタイム書き込みが1か月分丸ごと落ちていた（メインSSが正データなので復旧はできたが、気付くまで広告主シートは空だった）。
- Consequence: 25日実行なので月初0時の書き込みと作成が競合しない。**トリガーは最新の保存コードで動くため、本番デプロイ(@101)を更新しなくても有効**。
- Note: `createAdvertiserMonthSheet` のテンプレートを「最古の月シート」→「最新の月シート」に変更した。列構成を変えたとき新しい月に引き継がれるようにするため。

---

## 2026-07-30: 広告主シート保守ルート（adv_admin）を一時デプロイに用意

- Decision: `doGet` に `adv_admin=<キー>&action=preview|rebuild|ensuremonths` のキー保護ルートを追加。一時デプロイ `AKfycbwn--vmqogeWbeonVQ_gRzT_VD93jWyCfvDrw2drtRF-e7Un6wJ-jjhuc4xdNIg5Ibu` 経由で叩く。
- Reason: 月指定の再生成をチャットから実行・検証するため。`clasp run` は使えず、メニュー実行は手動になる。
- Safety: **本番デプロイ `AKfycbznoqLyw…`(@101) には入れていない**（`quest_admin` と同じ方針）。`action=rebuild` は月指定の再生成だけを行い、任意関数の実行はできない。
- Consequence: 緊急クエスト終了後（2026/08/01以降）に `quest_admin` を撤去する際、`adv_admin` と一時デプロイもあわせて撤去し、そのうえで本番を再デプロイする。

---

## 2026-06-12: 広告主シートの列構成を5列→6列に拡張（広告名追加）

- Decision: 広告主成果管理シートにB列「広告名」（フォーム表示名）を追加。列構成をA=受信日時, B=広告名, C=お名前, D=紹介者名, E=スクショURL, G=承認とした。
- Reason: 広告主から「どの案件の申請か」を判別できるようにする要件が発生。
- Consequence: `writeToAdvertiserSheet` の引数と `importExistingToAdvertiserSheet` のバッファ構造を変更。既存データは再インポートで上書き。

## 2026-06-12: 広告主シートの承認列をF→G列に変更

- Decision: 承認区分（GAS書き込み）をF列からG列に移動。
- Reason: 広告主がF列に「トラッキング漏れ」列を追加したため、GASの書き込み対象をG列にずらした。
- Consequence: `writeToAdvertiserSheet` および `importExistingToAdvertiserSheet` 内の列番号を6→7に変更。

## 2026-06-29: 広告主シートのトラッキング漏れを承認記号から反映

- Decision: 既存データを広告主成果管理SSへインポートする際、メインSSの承認列が⭕/○系ならG列「承認区分」、❌/✖/×系ならF列「トラッキング漏れ」にチェックする。
- Reason: 広告主提出用シートで、非承認のうちトラッキング漏れを明確に区別する必要があるため。
- Consequence: △や空欄など他の値は無視し、F/Gともfalseにする。リアルタイム新規書き込みは承認情報がまだないためF/Gともfalseで追加する。
- Operation: 2026-06-29に広告主成果管理SSをバックアップ後、202604=18件、202605=163件、202606=160件を再インポート。合計341件、承認139件、トラッキング漏れ57件、無視145件。

## 2026-06-12: 広告主シートインポートの行構造を確定（行2=ヘッダー、行3以降=データ）

- Decision: 広告主シートは行1がタイトル、行2がヘッダー、行3以降がデータ。インポート時は行3以降を `clearContent` してから書き込む。
- Reason: 当初 `deleteRows` を使ったところ「固定されていない行をすべて削除できない」エラーが発生。また `startRow=2` で上書きするとヘッダーが消えた。
- Consequence: `clearContent` を使い、書き込み開始行は `getAdvertiserNextRow()` で動的に決定（ヘッダーのA列が非空なので常に3行目を返す）。

## 2026-06-12: 月別シート自動作成機能の追加

- Decision: `importExistingToAdvertiserSheet` 実行時、対象月のシートが存在しない場合は既存の最古シートをコピーして自動作成する（`createAdvertiserMonthSheet`）。
- Reason: 202604シートを手動作成せずにインポートできるようにするため。
- Consequence: テンプレートコピー後に行3以降を削除し、月別シートを昇順に並べる処理も含む。

## 2026-06-12: 150件クエスト実装

- Decision: 全自社フォーム合計150件（2026/06末まで）を新クエストとして追加。3名（岩本拓也・菅原貴博・村井亮介）に各50件個人ノルマ。毎朝8時にLINE報告。
- Reason: 30件クエスト（4案件×30件）から拡張された新しい目標。
- Consequence: `quest150Report` 関数と専用トリガー `ensureQuest150Trigger` を追加。

## 2026-06-12: 顧客管理シートの自動連携

- Decision: フォーム送信時（doPost）に紹介者名が存在する自社フォームは、顧客管理シートへ自動upsertする。
- Reason: 営業マン別の顧客管理を手動入力なしで維持するため。
- Consequence: `upsertCustomerRow` を doPost から呼び出す。代理店フォームは対象外。

## 2026-06-12: 回答ヘッダー同期機能（fixAnswerHeaders）

- Decision: フォームにフィールドを追加した場合、`fixAnswerHeaders` を手動実行してヘッダーと既存データ行の列を修正する。
- Reason: リビンマッチに「地域」フィールドを追加した後、スクショURLが地域列に格納されていた問題が発覚。
- Consequence: 旧フォーマット行は新フォーマットに変換（空列を挿入してスクショURLを正しい位置に移動）。

## 2026-06-15: 新案件（自社フォーム）を顧客管理シートへ自動反映

- Decision: 顧客管理シートの案件列はシート作成時のみ確定していたため、新しい自社フォーム（案件）追加後も既存の顧客管理シートに反映されなかった。これを解消する。
- Reason: アフィリエイト管理で案件を増やしても、`upsertCustomerRow` が該当案件列を見つけられず（`caseCol <= 0` で早期 return）申請が反映されない問題があった。
- Consequence:
  - `syncCustomerManagementCases()` を新設（メニュー「顧客管理の案件列を同期」）。各営業マンシートのヘッダーと最新案件（`getJishaForms()`）を突き合わせ、不足案件列を書式・幅・入力規則付きで追加。
  - `upsertCustomerRow` を改修し、案件列が無ければ自動追加（`addCaseColumnToSheet_`）してから書き込む。これで以後の新案件はフォーム送信時に自動反映される。
  - デプロイは s***e@gmail.com で `clasp push`→`clasp redeploy AKfycbznoqLywTwLGrictq4dTKkbx5kcfn8g8PF60QpRdjgGaOCqUTuQLlfvE3hiWkYrLBlr`。誤アカウント（3s3.cube）でのデプロイは executeAs の都合で本番破壊につながるため厳禁。

## 2026-06-22: 入力途中保存と紹介者固定リンクを全案件共通にする

- Decision: 静的フォーム側で入力途中データを `localStorage` に自動保存し、`?form=<フォーム記号>-s/-m/-i` の紹介者固定リンクを全フォーム共通で解釈する。
- Reason: ユーザーがアフィリエイトリンク先で10分以上作業して戻った際に入力内容が消える問題と、紹介者選択ミスの多発を防ぐため。
- Consequence:
  - `-s` は `菅原貴博`、`-m` は `村井亮介`、`-i` は `岩本拓也` として紹介者欄を固定する。
  - GASへ設定取得するときは末尾サフィックスを外したフォーム記号を使うため、新規案件でも同じURLルールが使える。
  - スクショファイルはブラウザ仕様で復元不可。送信前に再選択が必要。
  - 本番は GitHub Pages (`origin/master`) に `c6e0b07` として反映済み。

## 2026-07-16: アフィリエイト報酬 振込先フォームを新設（独立GASプロジェクト）

- Decision: お客様から振込先情報を集める Google フォームを新規作成し、送信ごとに統合顧客管理ブック(SS1 = `1aaiCIDQIkrp_Ado5aKua_PTEQq4jr1UWqpuLQXwpemI`)の各営業担当タブ「総合_<担当>」とマスター「統合顧客管理」の、`持ち家かどうか`(K列)の直後に自動追記する。
- 実装場所: **既存の本番GASプロジェクトとは分離した独立スタンドアロンプロジェクト**（scriptId `1X30nki2jpev7aD7EjVgg2xrpmwEBOCeW5a0ZgR07RD-KDLpA3tpuWHj_`、ローカル `payout-form/`）。本番Webアプリ(申請フォーム側)を触らずに済むため。
- Reason: アフィリエイト報酬の支払いに振込先が必要。営業マンが手入力せず、お客様のフォーム回答から自動で顧客行に紐づける。
- 照合方式: フォームのフルネームを `normalizeName`(全角/半角スペース・不可視文字除去＋ひらがな化＋小文字化)で正規化して突合。完全一致→先頭一致(苗字のみ等)→レーベンシュタイン距離1、の順であいまい照合。紹介者(営業担当)は8名から選択式で入力させ、同名顧客の絞り込みに使う。
- 追記列(6列): `金融機関名 / 支店名 / 預金種目 / 口座番号 / 口座名義(カナ) / 振込先登録日時`。口座番号・店番の**先頭ゼロ落ちを防ぐため書き込み前にセル書式を`@`(テキスト)にする**。
- 耐久性: 「総合_<担当>」は `buildIntegratedRepSheets()` が `tab.clear()` で再生成するため、振込先が消えないよう**マスター(統合顧客管理)にも同時書き込み**する。マスターに列があれば再生成時に総合_タブへ複写されて保持される。既存の `buildIntegratedRepSheets` は列名でインデックスを引くため、6列追加でも破綻しない。
- 照合できない回答は SS1 の「振込先_未照合ログ」タブに記録する。
- 認証の要点: **clasp login はForms/Sheets等のセンシティブスコープをGoogleがブロックする**ため使用不可。スクリプトエディタで `runSetupAndSelfTest` を1回実行してオーナー(shinhogle)承認。検証・後始末はキー保護の一時Webアプリ経由で自走し、完了後にデプロイ削除・doGet/webapp設定をソースから除去済み。
- フォームURL(回答用): `https://docs.google.com/forms/d/e/1FAIpQLSd3AXOJrEwker1hKLATVtQpIbSZ4wWvAS5cAl-bbYQ46UKbZg/viewform`
- E2E検証済み: 「田中 太郎」(スペース入り)→「田中太郎」照合成功、マスター＋総合_藤森宣哉の両方に先頭ゼロ保持で書込→クリア確認。トリガー(onFormSubmit, shinhogle所有)発火も確認。

## 2026-07-16: 営業名簿を単一の変更点に集約し 江口裕人 を追加（本番反映済み）

- Decision: 営業担当（紹介者）の一覧を1箇所に集約し、メンバー増減を最小操作で行えるようにする。
  - 本体GAS: 定数 `JISHA_REFERRER_OPTIONS`（Code.gs）を唯一の名簿とする。`repStatusRepAliasMap_()` は名簿から正規名の自己対応を自動生成し、苗字・表記ゆれのみ手動 `variants` で補う方式に変更。
  - 振込先フォーム: `payout-form/Code.gs` の `SALESPEOPLE` を名簿とし、`setup()` 再実行で既存Googleフォームの「紹介営業担当」選択肢を `syncFormChoices_()` で同期。
  - フロント: `index.html` の `REFERRER_SUFFIX_MAP` に `-e → 江口裕人` を追加（固定紹介者リンク用）。
- 追加メンバー: `江口裕人`（本体名簿・振込先名簿とも計9名）。
- 運用: メンバー増減は名簿（本体は `JISHA_REFERRER_OPTIONS`、振込先は `SALESPEOPLE`）を編集し、本体はメニュー「フォーム管理 > 営業担当を同期」(`syncSalesRoster()`)を1回、振込先はエディタ or 一時Webアプリで `setup()` を1回実行する。`syncSalesRoster()` は非破壊で (1)全自社フォームの紹介者選択肢を再適用 (2)顧客管理SSに担当タブ追加 (3)SS2に担当タブ追加 (4)SS1に「総合_<担当>」タブ追加 を行う（タブ内データの再生成はしない＝空タブで用意し、`buildSalesRepStatusSheets`/`buildIntegratedRepSheets` の別実行で埋まる）。onOpen でも名簿変更を自動検知して紹介者選択肢を再適用。
- 江口裕人 追加時の本番反映（2026-07-16, version 101）: 上記4系統すべてに江口タブ/選択肢が揃ったことを確認済み。顧客管理SS・SS2・SS1(総合_江口裕人)・全14フォームの紹介者選択肢・振込先フォーム選択肢、いずれも江口裕人を含む。SS1の`総合_江口裕人`は空タブ（初案件が入り `buildIntegratedRepSheets` を実行すると埋まる）。
- 本番反映（2026-07-16, shinhogle）:
  - 本体GAS `clasp push`→`redeploy AKfycbznoqLywTwLGrictq4dTKkbx5kcfn8g8PF60QpRdjgGaOCqUTuQLlfvE3hiWkYrLBlr`（URL維持, version 99）。
  - `syncSalesRoster()` を一時メンテフック経由で実行し、紹介者選択肢を14フォーム更新・顧客管理SSに「江口裕人」タブ追加を確認（SS2は9タブ既存で追加なし）。実フォーム `?form=ouchikuraberu` の referrer が9択・江口裕人含むことを確認。一時フックはソース・本番とも除去済み。
  - 振込先フォームGAS `clasp push`、一時Webアプリ経由で `setup()` 実行→紹介営業担当が9択・江口裕人含むことを確認。一時Webアプリはundeploy・webapp設定/ doGet はソースから除去済み。
- 注意: 本コミットの `gas-project/Code.gs` には、前セッションで未コミットだった営業担当別ステータス表機能（`buildSalesRepStatusSheets`/`buildIntegratedRepSheets`/`repStatusRepAliasMap_` 等・SS2/SS1生成）が含まれる。当作業はこの `repStatusRepAliasMap_` を名簿駆動へ改修する形で同ファイルに乗るため分離不能。今回のデプロイ(version 99)でこれらの関数も本番に載ったが、いずれもトリガー無しの手動関数で、`buildSalesRepStatusSheets`（ゲート付きレビュー/実行待ち）は今回実行していない（休眠状態）。

## 2026-07-24: メンバー追加を「名簿編集＋同期1回」で完結させる（総合_江口裕人 が空だった件の恒久対策）

- 症状: SS1 の `総合_江口裕人` が空のまま更新されていなかった（ヘッダー行のみ）。`統合顧客管理` には 営業担当=江口裕人 の行が35件あった。
- 原因（コードのバグではない・手順の穴）: `syncSalesRoster()` は非破壊設計で**空タブを作るだけ**で、中身は `buildSalesRepStatusSheets()` / `buildIntegratedRepSheets()` の**別実行**が必要だった。この2関数はメニューにもトリガーにも無く、スクリプトエディタからの手動実行のみ。2026-07-16 に江口を追加した際に空タブが作られたあと再生成が走らず、そのまま放置されていた（ROADMAP の「残タスク=江口裕人 追加後の再生成」が未消化）。実際には江口だけでなく**全担当タブが古い**状態だった（例: 岩本 124行→169行、菅原 43→46、村井 16→17、柳沢 11→14）。
- 調査で確認した「反映済み」の範囲: 本体GAS・振込先フォームGAS・フォーム顧客管理GAS はいずれも**本番=ローカルで一致し 江口裕人 を含む**（`clasp pull` で差分0を確認）。`index.html` の `REFERRER_SUFFIX_MAP` の `-e` も反映済み。フロント2リポジトリも push 済み。つまり欠けていたのは「担当別データの再生成」だけ。
- Decision 1: `syncSalesRoster()` が最後に `buildSalesRepStatusSheets()` と `buildIntegratedRepSheets()` を続けて実行するようにした。メンバー増減は「名簿を編集 → メニュー『営業担当を同期』を1回」で完結する。各 build は個別に try/catch し、片方が落ちても他方と (1)〜(4) の結果を失わない。実行結果（担当別行数）はアラートとログに出す。
- Decision 2: 2つの再生成関数をメニューに追加し、エディタを開かずに単体実行できるようにした（`担当別ステータス表を再生成（SS2）` / `総合_担当タブを再生成（SS1）`）。
- Decision 3（副次的に発見した2つ目の穴）: SS2 の担当タブは**案件列の横幅がバラバラ**だった（柳沢/大島/小椋/細川/藤森/江口=17列、岩本=18列、菅原/村井=20列）。`buildSalesRepStatusSheets` は**そのタブ自身のヘッダー**を見て案件列を引くため、列が無い案件の実績は `unmatchedCase` に計上されて**黙って捨てられていた**。対策として:
  - `ensureRepStatusCaseColumns_()` を新設。メインSSの自社フォームから案件表示名を集め、名簿の全担当タブに不足案件列を追記して横幅を揃える。`syncSalesRoster()` から再生成の前に呼ぶ。メニュー単体実行用に `syncRepStatusCaseColumns()` も追加（新フォーム追加後に使う）。
  - `ensureRepStatusTabs_()` の見本タブ選択を「先頭に見つかったタブ」から「**案件列が最も多いタブ**」に変更。従来は古い狭いタブを複製するため、新メンバーだけ案件列が欠けた状態で作られていた（江口が17列だった原因）。
- 本番反映（2026-07-24, shinhogle, 本体GAS）: `clasp push` 後、**一時デプロイを新規作成して**そこから `syncSalesRoster()` をキー付きメンテフック経由で実行。本番デプロイ `AKfycbznoqLyw…`(@101) は**一切触っていない**（@HEAD デプロイは匿名アクセス不可だったため、使い捨てデプロイ @102/@103 を作成→実行→`undeploy`）。フック・キーはソースからも本番からも除去済み（`clasp pull` で live==local・`maint` 参照0を確認）。デプロイ一覧も元の7件に復帰。
- 実行結果: SS1 `総合_江口裕人` = 32行（0→32）。SS2/SS1 の全担当タブを再生成し、`unmatchedCase` は `{"出会えるエージェント":1}` → `{}` に解消。SS2 全9タブの案件列集合が完全一致（20列）になったことを確認。
- 未解決（ユーザー判断待ち）:
  - `excludedReferrers: {"藤井勇大": 3}` — 自社フォーム回答に紹介者名「藤井勇大」が3件あるが名簿に無く、SS2 で除外されている。新メンバーなら `JISHA_REFERRER_OPTIONS` に追加、誤入力なら回答側を修正する必要がある。
  - SS2 の `江口裕人` は0行のままだが、これは正常。SS2 は**フォーム回答の紹介者名**を集計する表で、江口を紹介者とする回答がまだ無いため。江口の32件は `統合顧客管理` の営業担当列由来で、SS1 側に正しく出ている。

## 2026-07-24: 藤井勇大 を営業名簿に追加（計10名・新手順の初適用）

- 背景: 同日の再生成で `excludedReferrers: {"藤井勇大": 3}` を検出。自社フォーム回答に紹介者名「藤井勇大」が3件あるが名簿に無く、SS2 から除外されていた。ユーザー判断により正式メンバーとして追加。
- 変更箇所（名簿の単一の変更点のみ）:
  - `gas-project/Code.gs`: `JISHA_REFERRER_OPTIONS` に `藤井勇大` を追加。`repStatusRepAliasMap_()` の `variants` に `"藤井勇大": ["藤井"]` を追加。**藤森宣哉 と苗字が近いが、`resolveRepCanonical_` は `normalizeName` の完全一致のみで引く**ため衝突しない（前方一致・あいまい照合は使っていない）。
  - `payout-form/Code.gs`: `SALESPEOPLE` に追加。
  - `フォーム顧客管理/main.js` と `index.html`: `SALES_STAFF` に追加、`SALES_STAFF_ALIASES` に `'藤井': '藤井勇大'` を追加（2ファイル同一内容を維持）。
  - `index.html`(アフィリンク フロント) の `REFERRER_SUFFIX_MAP` は**未変更**。固定紹介者リンクは現状4名(-s/-m/-i/-e)のみで、必要になったら記号を割り当てる。
- 実施手順（改修後の新手順を初めて通しで適用）: 名簿を編集 → `clasp push` → メニュー相当の `syncSalesRoster()` を1回実行、で全系統が揃った。旧手順のような再生成の別実行は不要になっている。
- 本番反映（2026-07-24, shinhogle）:
  - アフィリンク本体: `clasp push` 後、使い捨てデプロイ(@104)経由で `syncSalesRoster()` を実行し `undeploy`。**本番 `AKfycbznoqLyw…`(@101) は未変更**。フォームの紹介者選択肢は設定シートのデータ由来（`readConfig` が列6を読む）で、doGet/doPost は `JISHA_REFERRER_OPTIONS` を参照しないため、本番の再デプロイは不要と判断した。
  - 振込先フォーム: `appsscript.json` に webapp 設定と鍵付き `doGet` を一時追加 → `clasp push` → 使い捨てデプロイ(@4)で `setup()` 実行 → `undeploy`、フックと webapp 設定をソースから除去して再 push。デプロイは元の1件(@HEAD)に復帰。
  - フォーム顧客管理: `clasp push` → `clasp redeploy AKfycbxqy6u9…`(@18→@19、URL維持)。index.html は GAS が配信するため再デプロイが必要。
- 結果:
  - 紹介者選択肢17フォーム更新。顧客管理SS に `藤井勇大` タブ追加、SS1 に `総合_藤井勇大` タブ追加（SS2 の担当タブは既存だった）。
  - SS2 `藤井勇大` = 1行 / SS1 `総合_藤井勇大` = 1行。**`excludedReferrers` が `{}` に解消**（3件の回答は同一顧客のため集計行は1）。`unmatchedCase` も `{}` のまま。
  - SS1 `柳沢悠貴` が 14→13行。該当顧客のアフィリンク紹介者が藤井勇大のため、担当がアフィリンク優先で付け替わったもので設計どおり。
  - 振込先フォーム実物の選択肢が10名になったことを公開URLのHTMLで確認。
  - 3プロジェクトとも `clasp pull` で live==local・フック残骸0を確認。

## 2026-07-27: 特別緊急クエスト（ノムコム・30件）を緊急クエストと並走で開始

- 背景: 同日開始の緊急クエスト（5案件・全体80件・7/27 10時〜7/31）に加え、ノムコム単独の特別枠を7/31まで走らせる依頼。全体30件、内訳は 岩本拓也15 / 菅原貴博10 / 江口裕人3 / 藤井勇大2（合計がちょうど30）。
- Decision 1: 既存の緊急クエストに項目を足し込まず、**独立したレポート**（`specialQuestReport` / `buildSpecialQuestMessage_` / `ensureSpecialQuestTriggers`）として実装した。1通のメッセージに両クエストを詰めると長大になり、終了後の撤去も互いに絡むため。送信時刻は緊急クエストと同じ毎日13時・20時で、LINEには2通が並ぶ。
- Decision 2: 集計対象は `SPECIAL_QUEST_FORM = "nomukomu"`（表示名 ノムコム、自社=house）の1シートのみ。`getConfigSheetByCode` で直接引く。期間は `SPECIAL_QUEST_START_AT = 2026/07/27 00:00:00` 〜 `SPECIAL_QUEST_END_STR = 2026/07/31 23:59:59`。**本日分から数える**ため、それ以前の既存11件は対象外。
- Decision 3: 紹介者名の照合は緊急クエスト（`normalizeName` 完全一致のみ）ではなく `repStatusRepAliasMap_()` + `resolveRepCanonical_()` を再利用した。「岩本」など苗字のみ、「松田恵美（岩本拓也）」など括弧内表記も正規名へ寄る。名簿10名の正規名以外は個人ノルマに載らず、全体件数のみに入って「その他メンバー」行に出る。
- Decision 4: 管理ルート（`?quest_admin=<キー>`）に `quest=special` を追加し、`preview|send|setup|status` を緊急/特別で切り替えられるようにした。二重送信ガードのプロパティも `SPECIAL_QUEST_LAST_SENT` で独立。
- 本番反映（2026-07-27, shinhogle）: `clasp push` 後、**一時デプロイ `AKfycbwn--vmqogeWbeonVQ…` を @108→@109 に redeploy**（管理ルート専用の使い捨て。本番 `AKfycbznoqLyw…`(@101) は未変更）。旧@108は古いコードを配信していてクエスト切替が効かなかったため、redeployが必須だった。
- 検証: `action=preview&quest=special` で本文生成（622文字・改行20・リテラル `\n` 0件）を確認 → `action=setup&quest=special` でトリガー2本作成 → `action=send&quest=special` でLINEグループへ初回送信成功。緊急クエスト側のトリガー2本・`lastSent` が無傷であることを `action=status`（quest未指定）で確認。
- 初回時点の実績: 全体3件（岩本拓也1・菅原貴博2）。
- 撤去予定: 7/31終了後、両クエストのトリガーは初回実行時に自動削除される（`todayJst > END` で `remove…Triggers_()`）。管理ルート・一時デプロイ・定数ブロックは手動で撤去する。

## 2026-07-30: 振込先(口座情報)が顧客行に入らず、統合ビューでは列ごと消えていた

- Problem: 振込先フォームの回答が顧客行に反映されず、`統合顧客管理` の振込先列も空のままだった。原因は**2つ別々**にあった。
  1. **書き込み側（このプロジェクト）**: `findMatchRow_()` の照合は名前の正規化＋レーベンシュタイン近似のみで、申込時と振込先フォームで下の名前の表記が違うと当たらない。実際に届いていた2件（`押谷春美` / 担当 菅原貴博）は顧客行が `押谷沙良` だったため両方とも `振込先_未照合ログ` 行きになり、**顧客行には一度も書かれていなかった**。
  2. **消去側（フォーム顧客管理）**: `rebuildIntegratedCustomers()` が組み立てるヘッダーに振込先列が無く、`writeIntegratedSheet_()` の `clear()` で列ごと消していた。振込先列はこのプロジェクトが `ensurePayoutColumns_()` で「持ち家かどうか」の直後に挿入するため、再構築のたびに列が消える構造だった。→ [[../フォーム顧客管理/DECISIONS.md]] 同日エントリ
- Decision 1: 弱一致フォールバック `findSurnameMatchRow_()` を追加。**姓（先頭2文字）が一致 かつ 営業担当が紹介営業担当と一致 かつ 候補がちょうど1件**のときだけ書き込む。候補が0件でも2件以上でも必ず見送る。誤送金は取り返しがつかないため、曖昧なら書かないを既定にした。実データでは同姓グループが15件（`山本` ×4 が同一担当など）あり、これらは正しく自動判定を拒否する。
- Decision 2: 弱一致で書いた場合は**必ず**未照合ログに「要確認: 姓と担当のみ一致 <書込先>」として残す。自動で入ったことを人が後から検証・取消できるようにするため（取消は `clearPayoutForName()`）。
- Decision 3: 再照合 `retryUnmatchedPayouts()` を新設。照合ロジックを直したり顧客行を追加・改名した後に1回実行すれば、過去の取りこぼしが顧客行へ入る。解決済み行は理由列を `解決済…` に書き換えるので何度実行しても二重処理にならない。**振込先登録日時は再照合した日ではなく元の受信日時を保つ**（顧客が登録した日を残すため）。
- Decision 4: 担当タブの探索は**全タブを厳密照合してから**弱一致に落とす。先に弱一致で当たったタブが、後ろのタブの厳密一致を追い越さないようにするため。
- 本番反映（2026-07-30, shinhogle）: `clasp push` 後、`appsscript.json` に webapp 設定＋鍵付き `doGet` を一時追加 → 使い捨てデプロイ `AKfycbxCX_0-…`(@5) で `retryUnmatchedPayouts()` を実行 → `undeploy`、フックと webapp 設定を除去して再 push。デプロイは元の1件(@HEAD)に復帰。**7/24 と同じ手順**。
- 検証: 未照合2件が `統合顧客管理#238` と `総合_菅原貴博#19`（ともに `押谷沙良`）へ解決。新しい 7/30 の三菱UFJ が最終値で、登録日時は受信日時 `2026/07/30 13:57:02` を保持。ログは「解決済(要確認: 姓と担当のみ一致)」。`clasp` 側で live==local・`doGet`/`webapp` 残骸0を確認。
- ユーザー判断: `押谷春美` を顧客 `押谷沙良` と同一人物として紐づけることを承認済み（7/17の回答に本人が「押谷春美(もしくは 押谷沙良)」と併記、名義=オシタニハルミ、担当も両方 菅原貴博、顧客リスト283行に押谷姓は1件のみ）。
- 後続確認（2026-07-30）: フォーム顧客管理側の `rebuildIntegratedCustomers()` を本番で1回実行し、**復旧した振込先が再構築を跨いで残ること**を実データで確認済み（行238→282へ移動しても6値が一致、他人への誤付与0件）。詳細は [[../フォーム顧客管理/DECISIONS.md]] 同日エントリ。

## 2026-07-30(追補): 過去の振込先が反映されていなかった真因と一括復旧

- Problem: 未照合2件を復旧した後も「過去に登録した人が反映されていない」との指摘。調べると**振込先フォームは回答先スプレッドシートを持たず、回答の実体は Google フォーム本体にしかない**（`setup()` は `FormApp.create` するだけで destination を設定していない）。つまり:
  - `onSubmit` で**書き込みに成功した回答は、どこにも記録が残らない**（未照合ログは失敗分だけを記録する）。
  - そのため統合顧客管理の再構築で振込先列が消えると、**成功していた回答ほど痕跡ゼロで失われる**。未照合ログが2件しか無かったのはこのため。
- 実態: フォームには**18回答**（17名分・押谷が2回）。うち16名分は当時ちゃんと書けていて、再構築で消えていた。未照合ログに残っていた押谷2件だけが「そもそも書けなかった」分だった。
- Decision: フォーム回答を読み直して顧客行へ適用し直す `reapplyAllFormResponses(dryRun)` を新設。**過去分をまとめて復旧できる唯一の経路**なので、その場限りのスクリプトではなくコードとして残す。
  - 回答は**古い順**に適用する。同一人物が複数回登録していれば最新が最終値になる。
  - 振込先登録日時は再適用日ではなく**その回答の送信日時**を入れる。
  - `dryRun=true` で列追加も書き込みもせず「どこに入るか」だけ返す。復旧前の下見用。`writePayoutAll_` に `dryRun` 引数を足して実現した（照合ロジックを二重に書かないため）。
- 実行結果（2026-07-30, 使い捨てデプロイ `AKfycbze36kY…`(@6) 経由、実行後 undeploy＋フック除去＋live==local 確認）: 18/18 成功・失敗0。統合顧客管理と総合_菅原貴博にそれぞれ17行。**口座番号の先頭ゼロも保持**（0925531 / 0704915 / 0216773 で確認）。
- 注記: `菊池宏行` はフォームの紹介担当が「村井亮介」だが SS1 の営業担当は「菅原貴博」。担当タブ探索の全タブフォールバックが `総合_菅原貴博#39` を正しく引き当てた。`総合_村井亮介` にいる `菊池圭子` は別人で、`namesMatch` が編集距離2として正しく弾いている（振込先0件のまま）。
- **恒久対策の候補（未実施）**: フォームに回答先スプレッドシートを紐づけておけば、次に同種の消失が起きても回答台帳から復旧できる。現状は `reapplyAllFormResponses()` が代替手段。

## 2026-07-30(再発防止): 振込先フォームに回答台帳を持たせた

- Decision: フォームの回答先スプレッドシートを SS1 に紐づけ、回答タブを `振込先_回答台帳` に固定した（`ensureResponseDestination()`）。**回答の実体がGoogleフォーム本体にしか無い状態を解消**し、追跡経路を2本にする。
  - 経路1 = `振込先_回答台帳`（フォームが自動で書き込む生の回答・人手を入れない）
  - 経路2 = `振込先_未照合ログ`（顧客行へ書けなかった回答と理由）
  - 顧客行（統合顧客管理の振込先列）は**別GASプロジェクトの再構築で書き換わる**ので、唯一の記録にしてはいけない。この原則をファイル冒頭のヘッダーコメントにも明記した。
- 置き場所を SS1 にした理由: 振込先まわりの資産（`_payout_meta` / `振込先_未照合ログ` / 振込先列）が全て SS1 にあり、別ブックにすると管理対象が増えて紛失しやすいため。新タブ名は営業担当名を含まないので `getSalespersonSheets_()` にも `ensurePayoutColumnsAllSheets()` にも拾われない（拾われると振込先列を挿し込んでしまう）。
- `setup()` の新規作成・既存再利用の**両方の経路**から `ensureResponseDestination()` を呼ぶようにした。今後 setup() を回せば台帳は必ず用意される。
- 実行結果（2026-07-30, 使い捨てデプロイ `AKfycbyM4uZo…`(@7) 経由、実行後 undeploy＋フック除去＋live==local 確認）:
  - 紐づけ時点で**既存18回答すべてが台帳へ取り込まれた**（ledgerRows 18 == formResponses 18）。過去分の台帳も同時に手に入った。
  - 口座番号の先頭ゼロも台帳側で保持（0925531 / 0704915 / 0216773）。
  - 2回目の実行は `linked:false` で何もしない（冪等）ことを確認。
- 注意: 回答台帳は Google フォームが書き込む領域。**人が編集・行削除しないこと**（フォーム側との対応が壊れる）。復旧が必要なときは台帳を直すのではなく `reapplyAllFormResponses()` を使う。

## 2026-07-30(メモ・未対応): SS1 がリンク共有で誰でも閲覧できる状態

**対応は保留。ユーザー判断で記録のみ（2026-07-30）。**

- 観測した事実: `https://docs.google.com/spreadsheets/d/1aaiCIDQIkrp_Ado5aKua_PTEQq4jr1UWqpuLQXwpemI/export?format=xlsx` を**認証なし**で叩いてブック全体をダウンロードできた。つまり SS1 は最低でも「リンクを知っている全員が閲覧可」になっている（正確な共有設定そのものは未確認。確認できたのは匿名で読めるという挙動）。
- 何が読めるか: `統合顧客管理`（17名分の金融機関名・支店名・口座番号・口座名義）、`振込先_回答台帳`（18回答の生データ）、`振込先_未照合ログ`、各 `総合_<担当>` タブ、`顧客情報` の顧客個人情報一式。**口座情報が URL を知るだけで読める。**
- 今回の変更で増えた露出ではない（`統合顧客管理` の振込先列は以前から同じブックにあった）。回答台帳の追加で「同じブック内に同じ情報がもう1タブ増えた」だけで、閲覧できる範囲は変わっていない。
- 絞るときに壊れうるもの（先に確認が要る）:
  - **この作業で使っている検証手段が使えなくなる。** clasp の OAuth トークンは `drive.file` しか持たず SS1 を読めないため、シートの中身確認は匿名エクスポートと gviz に依存している（[[gviz_read_shared_sheet_tab]]）。絞るなら別途 `spreadsheets` スコープを持つ認証経路が要る。
  - 営業担当が各自の `総合_<担当>` タブを見る動線。誰がどのアカウントで開いているかを確認しないと、絞った瞬間に見えなくなる人が出る。
  - 他ツールからの gviz 読み取り（[[../../../CLAUDE.md]] 配下の各所で使っている手法）。
- 絞る場合の方向性: ブックを「リンクを知っている全員」から「特定のユーザー」へ変更し、営業担当と管理者を個別に招待する。口座情報だけを別ブックへ隔離して SS1 からは IMPORTRANGE も張らない、という分離も選択肢（ただし `ensurePayoutColumns_` / 再構築の前提が変わるのでコード修正が要る）。

## 2026-08-05: 成果管理シートの承認区分・トラッキング漏れは全月一致（定期確認）

- 確認方法: 一時デプロイ `AKfycbwn--…`(@112) の `?adv_admin=<キー>&action=preview&months=202604,202605,202606,202607,202608`。**書き込みを伴わない**。メインSS（正）の集計と広告主シートの現況を同時に返すので、この1回でズレの有無が判定できる。gviz でシート名指定して数えると別の月を読む事故があるため使わない（2026-07-30の教訓）。
- 結果（2026-08-05 時点）: **5か月すべて 行数・承認・トラッキング漏れが完全一致。ズレ0。** 広告主へ提出できる状態。

  | 月 | 行数 | 承認 | トラッキング漏れ | 承認率 |
  |------|------|------|------|------|
  | 202604 | 18 | 6 | 1 | 33% |
  | 202605 | 163 | 91 | 49 | 56% |
  | 202606 | 171 | 88 | 39 | 51% |
  | 202607 | 179 | 40 | 13 | 22% |
  | 202608 | 12 | 0 | 0 | 0% |

- 202607 は 7/30 の再生成時点（158件・承認40・漏れ13）から行が21件増えて179件になったが、承認と漏れの数は変わっていない。増加分は 7/30〜7/31 のリアルタイム書き込み（承認=false固定）で入ったもので、メインSS側も同じ数なので不一致は生じていない。**再生成は不要。**
- 観測: 7/30以降、承認は1件も増えていない（202605〜202607 のいずれも数値が動いていない）。202607 の承認率22%は 202605(56%)・202606(51%) と比べて低い。
- **ユーザー確認済み（2026-08-05）: これは問題ない。** 承認が付くペースの問題であって、メインSS側の記入漏れではない。広告主シートはメインSSに正しく追随できており、**この観点での追加調査は不要**。今後の突合でも承認数の増減そのものを異常のサインとして扱わない（見るべきは広告主シートとメインSSの一致であって、承認の絶対数ではない）。
- 未実施のまま残っている ROADMAP の撤去タスク（8/1予定）: `quest_admin` / `adv_admin` の保守ルートと一時デプロイ @112 は**今も生きている**（今回の確認にはこれを使った）。さらに 8/3 に作られた `AKfycbwwrB9V…`(@115) でも `adv_admin` が匿名で 200 を返すことを確認した。**鍵付きとはいえ匿名到達できるURLが2本ある**ので、撤去時は両方を対象にする。

## 2026-08-05: 本番Webアプリが匿名403 → アフィリエイトフォームが 8/3 から停止している

- Problem: 本番デプロイ `AKfycbznoqLyw…`(@113) に匿名でGETすると **403「アクセスが拒否されました／ドライブ アクセス権が必要です」**。公開フォーム `https://kazu02.github.io/affiliate-form/` の `config.js` はこのURLを指しているため、一般ユーザーはフォームを開くことも送信することもできない。
- 裏付け: 広告主シート 202608 のA列（受信日時）の最終行が **2026/08/03 13:49:41**。以降 2日間 0件（8/1 は7件、8/3 は5件届いていた）。
- **Cause（確定）: オーナー以外のアカウントで本番デプロイIDを再デプロイし、Web App の実行ユーザーがそちらへ移った。** `executeAs: USER_DEPLOYING` なので、再デプロイした瞬間に実行ユーザーが「最後に redeploy した人」へ移る。そのアカウントは本scriptのOAuthスコープを承認していないため、匿名リクエストが全て403になる。**同日(8/3)に 市場作り/フォーム顧客管理 で発生・診断済みの障害とまったく同じ**（[[../フォーム顧客管理/DECISIONS.md]] 2026-08-03、[[clasp-account-switch]]）。あちらは当日修復されたが、**アフィリンク本番が同じ原因で壊れていたことは気づかれないまま2日間放置された**。
- 確定に使った証拠:
  - Apps Script API `projects.deployments.list` で全9デプロイの `entryPointConfig` を取得。**本番(@113)も含め全て `access: ANYONE_ANONYMOUS` / `executeAs: USER_DEPLOYING`**。→ アクセス設定の変更ではない（当初この線を疑ったが**否定された**）。
  - バージョン 113 / 115 / 117 の `appsscript.json` も全て `ANYONE_ANONYMOUS`。→ マニフェストでもない。
  - `config.js` の URL は本番デプロイID・`webApp.url` と完全一致（72文字）。→ URL の指し違いでもない。
  - リダイレクト連鎖: 正常な @115 は `302 → googleusercontent → 200`。本番 @113 は **リダイレクトすら発生せず即 403**。→ 実行前に弾かれている＝実行ユーザーの承認欠如と整合。
  - Drive API: script「アフィリエイトフォーム」の **owner = `s***e@gmail.com`**、`3***e@gmail.com` は **canEdit=true（編集者）**。編集者でも `clasp push`/`redeploy` は成功してしまうため、誤アカウントでのデプロイが起こりうる状態だった。
  - 本番デプロイの `updateTime` = `2026-08-03T06:58:27Z`（**JST 15:58**）。最終申請 8/3 13:49 の直後で、停止開始と符合する。同日の 3s3cube 認証バックアップ（`.clasprc.backup-3s3cube-20260803-2140.json`）も残っている。
  - 対照: 同じ原因で壊れ**当日修復済み**のフォーム顧客管理は現在 302（正常）、アフィリンク本番のみ 403。
- **バージョンを戻しても直らない**（403の原因はコードではなく実行ユーザーのため）。`clasp push` でも直らない。
- 復旧手順（コード変更なし・URL不変・バージョン不変）:
  ```
  clasp show-authorized-user   # s***e@gmail.com を必ず目視
  clasp redeploy AKfycbznoqLywTwLGrictq4dTKkbx5kcfn8g8PF60QpRdjgGaOCqUTuQLlfvE3hiWkYrLBlr -V 113 -d "オーナー再デプロイで403復旧"
  ```
  `-V 113` を明示するので、未コミットの customer-line-bridge 系コードは本番に載らない（前回懸念した「公開する版の選択」は発生しない）。
- 予防: このプロジェクトのデプロイ前に必ず `clasp show-authorized-user` で `s***e@gmail.com` を目視する。`automation/scripts/gas-accounts.json` にも `プロジェクト\市場作り\アフィリンク\gas-project = shinhogle` として登録済み。
- 教訓: **同種の誤操作は同じ作業セッション内で複数プロジェクトに波及しうる。** 1件見つけたら、その日に触った他のGAS Web Appも公開URLで疎通確認する。

## 2026-08-05: 公開リポジトリに保守ルートのキーが載っていた（未対応・push停止中）

- Problem: このプロジェクトの origin `https://github.com/Kazu02/affiliate-form` は **public**。
  `gas-project/Code.gs` に定数として書かれている `ADVERTISER_ADMIN_KEY` と
  `EMERGENCY_ADMIN_KEY` が、**認証なしで誰でも読める**。
- 確認方法（値は記録しない）: `curl` で `raw.githubusercontent.com` の
  `master/gas-project/Code.gs` を無認証取得し、両キーの定義行が含まれることを確認した
  （2026-08-05・170,673バイト取得）。`gh repo view` でも `visibility: PUBLIC`。
- なぜ public なのか: 公開フォーム `kazu02.github.io/affiliate-form` の GitHub Pages
  配信元がこのリポジトリだから。Pages のプロジェクトページは public リポジトリを要する。
  つまり **「フォームを公開する」ことと「GASのソースを公開する」ことが同じリポジトリで
  結びついてしまっている**のが構造的な原因。
- 影響: キーが読める状態で、保守ルートを載せた一時デプロイ
  `AKfycbwn…`(@112) と `AKfycbwwrB9V…`(@115) が**いずれも匿名で200を返す**。
  この2つが生きている限り、外部から広告主成果管理シートの再生成（上書き）や
  クエストのLINE送信を実行できる。本番デプロイ(@113)は現在403のため経路になっていない。
- 露出期間: `adv_admin` を追加したのが 2026-07-30、`quest_admin` は 2026-07-27。
  少なくともそれ以降 public に置かれている。
- **このリポジトリへの push は解消まで停止する。** 2026-08-05 のワークスペース整理では
  他のネストリポジトリ（ラボ会議・RAGNIZE管理画面・shopee・フォーム顧客管理）は push したが、
  ここだけ意図的に見送った（未push 5コミット）。新規に `CODEX_TASK.md` /
  `CODEX_REPORT.md` / `CLAUDE_REVIEW.md` を公開側へ足すことになるため。
- あわせて既に public な情報: `DECISIONS.md` / `PROJECT_CONTEXT.md` / `ROADMAP.md`
  （スプレッドシートID4種・スクリプトID・デプロイID・営業担当の実名を含む）、
  `config.js`（本番デプロイURL）、`payout-form/Code.gs`（キー等の直書きは無し）。
- 対応方針（未実施・要判断）: (1) 保守ルート自体を Code.gs から撤去して両キーを無効化する、
  (2) 一時デプロイ @112・@115 を削除して経路を閉じる、(3) GASソースとPages配信物を
  別リポジトリに分け、GAS側を private にする、(4) 既に公開された内部ドキュメントの扱い。
  履歴に残るため、キーは撤去だけでなく**別の値へ入れ替える**必要がある。


## 2026-08-05: 公開キーの経路を閉じ、ソースからキーを除去した（対応実施）

上の露出に対して2段階で対応した。

### (A) 経路を閉じた

保守ルートを載せた一時デプロイ2本を削除した。これで「キーが読める＋叩ける口が
開いている」という組み合わせが解消される。

- `AKfycbwn…`(@112 広告主シート過去月再同期) を削除
- `AKfycbwwrB9V…`(@115 customer-line-public-check) を削除
- 削除後、両URLとも **HTTP 404**。デプロイは9本→7本。
- 残る7本のうち @1〜@21 は管理ルート追加(7/27・7/30)より前の版で、ルートを含まない。
  `@HEAD` は匿名アクセス自体が「アクセスする権限がありません」で拒否される。
  本番 `AKfycbznoqLyw…`(@113) は現在403で到達不能。
  → **現時点で管理ルートに到達できる匿名の口は存在しない。**

### (B) ソースからキーを除去した

- `ADVERTISER_ADMIN_KEY` の直書きを廃止し、`ScriptProperties` の
  `ADVERTISER_ADMIN_KEY` から読む `advertiserAdminKey_()` に置き換えた。
  **未設定のあいだはルートを一切通さない**（`advAdminKey &&` で fail-closed）。
  使い続けたい場合は ScriptProperties に**新しい値**を入れる。露出した旧値は使わない。
- `EMERGENCY_ADMIN_KEY` と `quest_admin` ルートは撤去した。クエストは 2026/07/31 で
  終了しており、再開時も同じ値は使わない。`handleEmergencyAdmin_` 本体は
  到達不能な状態で残置（撤去は別途）。
- `clasp push` 済み。GAS上のHEADを再取得して**16進リテラル0件**・live==local を確認。

### 残っていること

- **リポジトリは依然 public で、git履歴には旧キーが残る。** 旧値を再利用しないことで
  無効化しているが、履歴からは消えていない。
- **本番復旧の手順が変わった。** 版113には直書きキーが残っているため、
  `-V 113` で戻すと露出が復活する。**復旧は現行HEADから新しい版を切って行う**こと。
  ただしHEADには customer-line-bridge の変更が含まれるため、その反映可否は
  顧客向け公式LINE側の判断と合わせる必要がある。
- 構造的な解決（GASソースとPages配信物のリポジトリ分離）は未着手。
- 定数比較は `===` のままで、タイミング差の対策は入れていない（露出解消を優先した）。


## 2026-08-05: 本番を復旧し、到達不能になったクエスト管理コードを撤去した

- 復旧: `clasp create-version` で **@118**（`admin-key-removed-owner-redeploy-2026-08-05`）を
  切り、**オーナー(`shinhogle`)認証のまま**本番デプロイID `AKfycbznoqLyw…` を @118 へ
  redeploy した。**`-V 113` は使っていない**（版113には直書きキーが残るため）。
- 復旧の確認（いずれも匿名・2026-08-05）:
  - `?form=ouchikuraberu` → **HTTP 200**、フォーム設定JSONを返す（8/3以降ずっと403だった）。
  - 最終応答に `Access-Control-Allow-Origin: *` / `Content-Type: application/json` があり、
    GitHub Pages からの `fetch` が通る。
  - 公開ページ `kazu02.github.io/affiliate-form/?form=ouchikuraberu` も 200。
  - **露出していた旧キーで `adv_admin` を叩いても管理応答は返らず**、通常のフォーム設定が
    返る（ScriptProperties 未設定 = fail-closed が効いている）。
- HEADから到達不能だった `handleEmergencyAdmin_`（47行）を削除した。参照は定義のみで0件。
  再開する場合もキーはソースに書かない。`node --check` 通過後に `clasp push`。
  **本番は @118 のまま**で、この撤去は挙動を変えないため再デプロイしていない
  （次に機能変更をデプロイするときに載る）。
- **停止期間: 2026/08/03 13:49 〜 2026/08/05 14:0x（約2日）。** この間の申請は
  フォーム自体が開けなかったため、記録に残らない取りこぼしがある可能性が高い。

### 保守ルートを再び使いたくなったら

`adv_admin` は残してあるが、`ScriptProperties` の `ADVERTISER_ADMIN_KEY` が未設定の
あいだは誰も通さない。使う場合はGASエディタの「プロジェクトの設定 > スクリプト
プロパティ」で**新しい値**を追加する（露出した旧値は使わない）。設定しないままでも、
広告主シートの確認・再生成はスプレッドシートのメニューから実行できる。


## 2026-08-05: 停止期間中の申請は「どこにも記録されていない」ことを全フォームで確認

ユーザーから「停止中の申請もおそらく届いているはず」との見立てがあったため、
広告主シート（自社フォームのみ）ではなく**正データであるメインSSの「管理」シート**で
全フォームを確認した。

- メインSS = `1JOMT_Uuoq3H6O9lZcAKuSpzC2ehUJ35k53mVvnZRwY0`（アフィリエイト管理）。
  「管理」シートに `フォーム記号 / フォーム名 / フォームURL / 代理店コード / 回答数 /
  最終回答日時` があり、**代理店コード付きフォームも含めて全28フォーム**を一覧できる。
  リンク共有のため gviz で認証なしに読める（共有設定は意図的なもの・変更しない）。
- 結果: **全28フォームの最終回答日時の最大値が `2026/08/03 13:49:41`**（ノムコム）。
  それより後の回答があるフォームは **0件**。広告主シート202608も12行で同じ時刻が末尾。
  → 停止期間に届いた申請は、自社フォーム・代理店フォームを問わず**どこにも記録されていない**。
- 直前の挙動と合わせると、doPost 自体が実行されていない（本番が匿名403）ため、
  メインSS・代理店SS・LINE通知・顧客管理・広告主シートの**いずれにも痕跡が残らない**。
  したがって記録からの復元はできない。
- 規模の見立て: 8月は 8/1=7件・8/2=0件・8/3=5件で日平均約4件。7月は179件（曜日別合計で
  木48・火29・水28・月21・土14・金9・日9）。**0件の日は珍しくない**（7月も8/2も実在）ので、
  停止した8/4(火)・8/5(水)に実際に何件の申請意思があったかは**原理的に不明**。
  失われたとしても一桁台と見られるが、ゼロだったとも言い切れない。
- 緩和材料: フォームは入力途中の値を `localStorage` に自動保存し、**送信成功時にのみ削除**する。
  停止中に送信を試みて失敗した人が同じ端末で開き直せば、入力内容は復元される
  （スクショの再選択のみ必要）。

**教訓**: 取りこぼしの有無を見るときは広告主シートだけを見ない。代理店コード付き
フォームは広告主シートの対象外なので、メインSSの「管理」シートで全フォームを見る。


## 2026-08-06: 7月分の請求対象を確定した（承認90件・6案件）

7月分の請求にあたり、対象案件を洗い出した。単価はユーザーが別途調べる。

- **集計の定義**: メインSS（正データ）の `設定_*` タブを対象に、**受信日時が 2026/07/01〜07/31**、
  かつ **承認列が ⭕ 系**（`getAdvertiserApprovalFlags` と同じ判定）の行を数える。承認日ではなく
  申請日（受信日時）を基準にした。代理店コード付きフォームは対象外だが、**7月は代理店フォームの
  受信が0件**なので今回は差が出ない。
- **請求対象（承認済み）: 6案件・合計90件**
  不動産一括査定イエイ 24 / 保険マンモス 23 / リビンマッチ 19 / いえカツLIFE 18 /
  RE-Guide(リガイド) 5 / HOME4U不動産 1
- 7月受信の内訳は 178件 = 承認90 / 承認欄が空 57 / 非承認31。空57件の内訳は
  **ノムコム48**・出会えるエージェント4・HOME4U家づくりのとびら3・HOME4U不動産2。
  ノムコムは7月の受信数が最多（48件）で全件が未入力のため、**単価次第で請求額が大きく動く**。
- 8/3〜8/5 の本番停止（匿名403）は8月の受信にのみ影響し、**7月分の完全性には影響しない**。

### 広告主シート 202607 は提出前に再生成が必要

- 現況: 広告主シート202607 = 179行・承認40・トラッキング漏れ13。メインSS = 178行・承認90・非承認31。
  **承認が40対90でずれている。**
- 原因は既知のもの（2026-07-30 の記録）。`writeToAdvertiserSheet` は承認=false固定なので、
  **後から付いた承認はリアルタイム書き込みに乗らない**。2026-08-05 の確認時点では両者とも承認40で
  一致していた＝**8/5以降に50件の承認が入力された**ということ。異常ではなく想定どおりのたまり方。
- 行数の1件差: 広告主シート側にイエイ 7/28 の同一氏名行が2行あり、メインSSは1行。再生成で解消する。
- したがって**広告主へ提出する前に 202607 を再生成する**（メニュー「広告主シートを月指定で再生成」）。
  上書きになるので事前にバックアップを取る。
- 調査手法: メインSS・広告主SSとも `…/export?format=xlsx` で**認証なしにブック全体を取得**でき、
  openpyxl で全タブを一度に集計できた。gviz のシート名指定のような誤読事故が起きず、`adv_admin`
  保守ルート（現在キー未設定で通らない）も不要。**読み取り専用の突合はこの経路が最も安全**。



## 2026-08-19: 全31案件のアフィリンクを実際に開いて稼働/停止を判定した（稼働13・停止18）

「広告主からもらうアフィリンクが開けるかどうか＝案件が動いているかの目印」というユーザーの
定義に沿って、メインSSの `設定_*` タブ31件のアフィリエイトURLを**実際に叩いて**判定した。

- **判定の根拠は明確な1つの分岐**。ASP（`ac.fairpublicrelations.jp` / `ac.conel.jp` /
  `ac.trictrac.jp`）のリンクは、生きていれば広告主のLPまでリダイレクトが通り、死んでいれば
  **ASPのトップへ302で戻され「このキャンペーンは終了しました。本キャンペーンは掲載が終了して
  いるか、URLに誤りがあります。」という固定ページ（1703バイト）**が出る。HTTPは両方とも200なので
  **ステータスコードでは判定できない**。本文の文言と最終到達ホストで見る。
- **稼働中 13件**: リビンマッチ / ノムコム / 保険マンモス / いえカツLIFE / スマモニ /
  RE-Guide(リガイド) / ポケットリサーチ / セゾンビジネスプラチナアメックスカード /
  セゾンマネーカード / SBI証券 ／（小中様）おうちクラベル・スマモニ・ポケットリサーチ
- **停止 18件**: 不動産一括査定イエイ / おうちクラベル / キレイライン / HOME4U不動産 /
  HOME4U土地活用 / HOME4U家づくりのとびら / みんなの生命保険アドバイザー(オンライン・対面) /
  SREリアリティー / アイネット証券 / 出会えるエージェント ／（小中様）アイネット証券・
  SREリアリティー・みんなの生命保険アドバイザー(オンライン・対面)・キレイライン・
  HOME4U土地活用・HOME4U不動産

### 効いている事実: 7月の請求対象6案件のうち2件が止まっている

2026-08-06 に確定した7月分の請求対象（承認90件・6案件）と突き合わせると、

- 稼働中: 保険マンモス(23) / リビンマッチ(19) / いえカツLIFE(18) / RE-Guide(5)
- **停止: 不動産一括査定イエイ(24) / HOME4U不動産(1)**

**イエイは7月の承認数が最多（24件）で、最終回答も 2026-08-01 と直近**。それが今は開かない。
HOME4U系は4案件すべて停止。ノムコム（7月受信48件・全件未承認）は稼働中。

### 「終了」と「リンクの貼り替え」を区別していない点に注意

ASPの文言は **「掲載が終了しているか、URLに誤りがあります」** で、両方を同じページで返す。
つまり *案件が本当に終わった* のか *リンクが再発行されて古いURLが無効になった* のかは、
この判定では**区別できない**。直近まで成果が出ていた案件（イエイ・HOME4U不動産・
HOME4U家づくりのとびら・出会えるエージェント）は、終了と決めつけず**ASP/広告主に現行URLを
確認する**のが先。長期に受信ゼロのもの（みんなの生命保険アドバイザー対面など）は終了と見てよい。

### 同じ広告主でもリンク単位で結果が割れる

**おうちクラベルは自社リンクが停止、小中様（代理店01kn）リンクは稼働**。広告主側はまだ
出稿しており、無効なのは自社のURLだけ。**代理店のリンクを自社で流用してはいけない**
（成果が代理店に付く）ので、自社用の現行URLを発行してもらう。

### 調査手法（再実行する人向け）

- メインSSは `…/export?format=xlsx` で認証なしにブック全体を取得し、openpyxl で全 `設定_*` タブの
  「アフィリエイトURL」を抜く（2026-08-06 に確立した経路と同じ）。**gviz の `sheet=` は名前を
  解決できないと黙って先頭シートを返す**ので使わない（今回も再現した）。
- リンクは `curl -L` だけでは足りない。**af-mark.jp / ganesha-asp.com / t.afi-b.com は
  meta refresh と `location.href` で次へ飛ばす**ため、HTMLを読んで手動で追う必要がある
  （追わないと中継ASPのホスト名で止まり、生きている案件を誤って「不明」に分類する）。
- ブラウザ相当のUAと `Referer: https://kazu02.github.io/...` を付けて再検証した。
  付けても付けなくても結果は同一で、参照元不足による誤検知ではないことを確認済み。
- **判定のたびにASPのクリックが1回計上される**。むやみに連打しない。

**残タスク（ユーザー）**: 停止18件のうち直近まで動いていた案件について、ASP/広告主へ
「終了したのか、URLが変わったのか」を確認する。新URLが来たらメインSSの該当 `設定_*` タブの
「アフィリエイトURL」を差し替えるだけで反映される（フォーム側の変更は不要）。


## 2026-08-19: アフィリエイト代理店の登録・承認まわり自動化の打ち合わせ

代理店の登録フロー・リンク配布・承認状況の見せ方・管理画面の範囲を決めた。

- **代理店の登録はフォーム完結型にする。** フォーム送信が完了したら、登録されたメールアドレスへ
  稼働中の案件が全部載ったアフィリエイトリンク生成ページが届く形にする。
  理由: 代理店側が自分で完結でき、「これがあなたのフォームです」と手元に届く方がよいと
  すがわらたかひろが判断したため。
- **今回登録してもらうのはアフィリエイト代理店をやる人だけに限る。**
  理由: 現時点で出せるリンクがアフィリエイトのものしかないため。
- **承認状況の一覧は代理店ごとにページを分けず、1ページに全代理店ぶんを並べる一元管理にし、
  代理店は色を変えて区別する。**
  理由: 代理店が10社に増えるとページが分かれ、全件が一行で並ぶASPの管理画面との突合が
  ややこしくなるため。
- **代理店にも新規申請の通知を送る。**
  理由: スクショに日時が入っていないなどの不備をその場で指摘して送り直してもらえ、抜けも
  こちらから言えるため。代理店として動いてもらう面でもプラスになるため。
- **アフィリエイト管理スプレッドシートに案件の稼働中チェックを設け、稼働にチェックが付いている
  案件だけを表示する。**
  理由: 案件が多すぎて携帯から見るとどれがどれか分からなくなるため。
- **小永（コナカ）さんの案件は稼働をバツにして表示から外す。**
  理由: 動く気がなさそうで、だいぶ前に作って以来何も触られていないため。
- **代理店向けの画面にはアフィリエイトリンクを作成する機能を作らない。リンクは sugawa kazuki が作る。**
  理由: リンクは sugawa kazuki に作ってもらう方がメンバーが動くため。代理店向けはLINEのノートの
  内容も見られる確認用程度でよいため。
- **管理画面はがっつりしたものではなく、リンクとそのリンクから登録した人数がわかる程度の一枚もので
  始め、徐々にアップデートしながら追加する。まずはフォームからリンクが生成されるところまで作る。**
  理由: 作っても代理店が動かなければ意味がないため、必要な範囲から始める。

出典: 01_資料/会議ログ/2026-08-19_アフィリエイト代理店の登録・承認まわり自動化の打ち合わせ/議事録.md

## 2026-08-19: 稼働ボタン・1案件1タブ化・代理店リンク集を実装した（本番 @119）

代理店が増えるとURL管理が破綻する問題に対し、提案（同日 ROADMAP）を実装した。目的は
**代理店へ稼働中案件のリンクを渡し稼働状況をリアルタイムに反映すること**と、
**代理店の顧客も含めて一元管理すること**の2つ。

### 決めたこと1: アフィリエイトURLは案件ごとに1本、全代理店で共有する

1案件1タブへ集約すると、代理店ごとのASPリンク（bId）を置く場所が無くなる。ユーザー判断で
**ASP側で代理店を分けるのをやめ**、代理店別の実績は自社スプシの「代理店」列で集計する方式にした。
これが「分けると代理店が増えたときに管理が大変」への直接の答え。ASPレベルでの分離が必要に
なった場合は設計が変わる。

### 決めたこと2: 稼働/停止の入力は案件マスタのチェックボックス1か所だけ

`案件マスタ` シートの「稼働」列（チェックボックス）が唯一の入力。ここから
(1) 設定タブの表示/非表示 (2) 代理店リンク集の掲載可否 (3) 申請フォームの受付可否
が同時に決まる。`onEdit` で押した瞬間に反映される。リンク集ページは開くたびにGASへ
問い合わせるので、チェックを外せば代理店側からも即座に消える＝リアルタイム更新の実体。

- 初期値は2026-08-19時点の稼働8件（ノムコム/セゾンビジネスプラチナアメックス/セゾンマネー/
  SBI証券/保険マンモス/リビンマッチ/RE-Guide/いえカツLIFE）。
- **スマモニとポケットリサーチはリンクが生きていたが、ユーザー判断で非稼働**とした。
  稼働判断はリンクの生死ではなく業務判断が正。

### 決めたこと3: 代理店専用タブを廃止し、識別は回答行の「代理店」列で行う

- 「代理店」列は**回答ヘッダーの末尾**に足した。末尾なので既存列の位置が動かず、既存データと
  承認列の読み取り（`getAdvertiserApprovalFlags` など）に影響しない。
- 集約の前に**小中様10タブが全て回答0行であることを実測**した。データ移行が発生しないことを
  確認してから構造変更に踏み切っている。
- 代理店リンクは `?form=<案件コード>&ag=<代理店コード>`。紹介者はその代理店の担当者ひとりに
  決まるので**紹介者欄を出さず**、GASが代理店マスタから入れる。
- 代理店経由の申請は referrer を代理店の担当者名に置き換えるため、**自社と同じ経路で顧客管理へ
  入る**＝顧客の一元管理。代理店登録時に顧客管理SSへ担当者タブを用意する。

### 決めたこと4: 代理店登録フォームは合言葉で保護する（fail-closed）

`agency.html` は誰でも開けるURLなので、**無防備だと任意のアドレス宛にオーナーのGmailから
メールを送らせられる＝送信の踏み台になる**。`ScriptProperties` の `AGENCY_SIGNUP_KEY` を
検証し、未設定のあいだは誰も通さない（既存の `ADVERTISER_ADMIN_KEY` と同じ方式）。
**値はソースへ書かない。このリポジトリは public。**

### 停止中の案件はフォームを出さない

従来は停止中でもボタンが出て、顧客が「このキャンペーンは終了しました」に着地して申請できずに
終わっていた（営業の信用に直結する事故）。`config.suspended` で案内文に差し替え、
**直接POSTされた場合もサーバ側で拒否**する。フォームの送信は `mode:"no-cors"` で応答を読めない
ため、画面側のガードが実質の防波堤になる。

### 実装上の注意点（次に触る人向け）

- **MailApp を足すと新しいOAuthスコープが要る。** 未承認のままデプロイすると、executeAs が
  USER_DEPLOYING のため**匿名アクセスが全て落ちる**（2026-08-03 と同じ形）。
  今回は「push → エディタで `setupCaseAgencyFeature()` を実行して承認 → deploy」の順で回避した。
  **push だけなら本番は無傷**（デプロイ版はバージョン固定のため）。ただし**トリガーはHEADを見る**
  ので、承認前に定期トリガーが走ると失敗する。
- 初回セットアップは `setupCaseAgencyFeature()` の1本にまとめた（権限承認・案件マスタ生成・
  稼働8件の投入・表示反映・代理店列追加・合言葉発行・小中様の停止登録）。
- `agency.html` の送信は `Content-Type: text/plain` にしてCORSプリフライトを避けている。
  これで応答を読めるので、メール送信に失敗してもリンク集URLを画面に出せる。
- **`管理` シートに稼働状況の列を足してはいけない**（`updateManagementSheet()` が
  `clearContents()` で6列に作り直すため消える）。稼働は `案件マスタ` が正。

### 検証

- 構文: `Code.gs` / `CaseAgency.gs` と3ページのインラインJSを `node --check`。`.gs` 全体で
  トップレベルの const / function 名の重複ゼロ。
- push 後・deploy 前に本番デプロイ版が無傷であることを実測（HTTP 200・旧config・suspended なし）。
- セットアップ後にブックを xlsx で取得して実測: 案件マスタ21行・稼働8件が指定と完全一致、
  **表示8/非表示23がシートの `sheet_state` と一致**、全31シートに「代理店」列あり、
  代理店マスタに小中様が停止で登録。
- deploy 後に匿名で実測: HTTP 200、稼働案件は `suspended=false`、停止案件（イエイ・
  おうちクラベル）は `suspended=true`、無効トークンのリンク集は拒否。

## 2026-08-19: 代理店への通知は当面メールにし、LINE化（統合アプリのagent役割）は設計だけ用意する

代理店への通知方法を検討し、**まずメールで運用を始め、LINEは必要になってから統合アプリの
既存 agent 役割に乗せる**と決めた（ユーザー判断）。

### メールを選んだ理由

- 送信の仕組みが既に動いており追加費用がない（MailApp・登録時のリンク集送付で実績あり）。
- **LINEのプッシュメッセージは従量**で、公式アカウントの無料枠は小さい。申請ごとに通知すると
  代理店が増えたときに上限に当たる。友だちからの発言へ返すリプライは無料なので、
  こちらから押す通知は最小限にする設計が要る。
- 代理店3〜4社の段階では通知量が少なく、メールで足りる。

### 変更検知を自動化した（手で送る運用にしない）

稼働状況を変えても手でメニューを押さない限り代理店に伝わらないと、**止めた案件のリンクを
代理店が配り続ける**／**始めた案件が使われない**が起きる。`notifyAgenciesIfCasesChanged()` を
日次トリガー（9時台）で回し、稼働案件の集合を前回と突き合わせて**変わったときだけ**
稼働中の全代理店へメールする。

- 前回値は ScriptProperties の `AGENCY_NOTIFIED_CASE_CODES`。
- **初回は送らずに現状を記録するだけ**にした。これをしないと「全案件が新規」という誤った内容を
  一斉送信してしまう。
- 送信が全滅したときは記録を進めない（次回に再試行させるため）。
- 終了した案件名は稼働一覧に居ないので、設定タブ側から名前を引いている。

### LINE化（案A）を選ぶ場合に効いてくる制約2つ

**当時の検討結果を残す。実装するときはここから始める。**

1. **代理店コードでの紐づけはなりすましができる。** コードは `ag01`, `ag02` と連番で推測可能。
   友だち追加して他社のコードを送れば、その代理店宛の通知を受け取れてしまう。
   **紐づけはコードではなく、登録時に発行済みのトークン（代理店マスタのトークン列）で行う。**
   使い捨てにし、紐づけ完了後は無効化する。
2. **GASの `doPost` はHTTPヘッダーを読めない**（`e` に `postData`/`parameter` はあってもヘッダーが無い）。
   したがって LINE の `X-Line-Signature` を検証できず、`/exec` のURLを知っていれば偽イベントを
   投げられる。**この案件で直接webhookを止めてブリッジ方式にしてあるのはこの理由**
   （`LineBridgeSecurity.gs` が本文にHMACを載せて検証する作りになっている）。
   よって LINE を足すなら **LINE → 統合アプリ（Vercel・署名検証あり）→ 署名付きブリッジ → 本GAS**
   の既存経路に乗せることになり、**アフィリンク単独では完結しない**。

### なぜ案B（社内向けボットを代理店専用に分ける）ではなく案Aか

統合アプリには**既に `agent` 役割のLINE紐づけが実装済み**で、旧代理店8件が登録されている
（紐づけ審査・管理者の代理承認・失効経路まで作られている）。案Bだと同じ仕組みを二重に作ることに
なる。どちらの案でも webhook の署名検証のため統合アプリ側の変更は避けられないので、
**既にある仕組みに乗る案Aの方が安い**。残る作業は代理店マスタ（本GAS）と統合アプリの
line_identities の対応付け。

## 2026-08-19: 代理店ごとの取扱案件・実績表示・申請状況の一元表示を実装した（@120/@121）

会議で挙がっていた残り3項目を実装した。

### 代理店ごとに渡す案件を選べるようにした

全稼働案件を一律に見せると、代理店によって扱いたい案件が違うためずれる。
`代理店別取扱案件` シートで **案件（稼働中）×代理店（稼働中）のチェック**を持ち、
リンク集をそれで絞る。

- **列が無い＝未設定のときは全稼働案件を渡す。** 設定前でも従来どおり動かすため。
- **新しい組み合わせは既定で「渡す」。** 代理店登録時に `syncAgencyCaseMatrix()` を呼んで列を
  作るので、何もしなければ全案件が渡り、外したい案件だけチェックを外す運用になる。
  ここで列を作らないと「リンク集は全案件で動き続けるのに設定場所が無い」状態になる。
- ヘッダーは `代理店名（コード）` で、**括弧内のコードを正として読む**。代理店名を変えても
  対応が壊れないようにするため。

### リンク集を一枚ものの管理画面にした

案件ごとの申請件数と合計を表示する。件数は案件タブの「代理店」列を数える。
**代理店名で突き合わせるので、代理店名を変えると過去分が数えられなくなる**（既知の制約）。
数えるキーをコードにしなかったのは、シートを人が読むとき名前の方が分かるため。

### 申請状況を1枚に集めて代理店で色分けした

`申請状況一覧` シート（生成物）。全案件タブの回答を集め、受信日時の新しい順に並べ、
代理店ごとに行の背景色を変える。自社は白のまま。

- **受信日時は古い行が Date、新しい行が文字列**なので両方を扱う（実データで確認済み。
  Googleスプレッドシートが `formatJST` の "YYYY/MM/DD HH:mm:ss" を日付型へ自動変換している）。
- 手で押さないと古いままになるので**日次トリガー（7時台）で作り直す**。申請のたびに
  作り直す案は、数百行の再生成が送信のたびに走るので採らなかった。

### 代理店へ新規申請を通知する

代理店リンク経由の申請が入ったら、その代理店へメールする。**顧客の氏名は本文に載せない**
（宛先を間違えたときの被害を小さくするため）。案件名と受付時刻、申請数の確認リンクだけを送る。
失敗しても申請の記録は止めない（doPost 側で try/catch）。

### 検証

- 全 `.gs` を `node --check`、グローバル名の重複ゼロ、`links.html` のインラインJSも構文検査。
- clasp のアカウントを目視してから push し、@120 と @121 をデプロイ。
- 各デプロイ後に匿名で回帰確認（稼働案件 suspended=false / 停止案件 suspended=true /
  無効トークンのリンク集は拒否 / HTTP 200）。

## 2026-08-20: 停止18件は「終了」で確定。代理店別サマリーを実装して代理店機能は完成

### 停止18件は終了で確定した（URLの貼り替えではない）

2026-08-19 の調査では、ASPの文言が「掲載が終了しているか、URLに誤りがあります」で
**終了とリンクの貼り替えを区別しない**ため、直近まで成果が出ていた案件（イエイ・HOME4U不動産・
HOME4U家づくりのとびら・出会えるエージェント）は保留にしていた。ユーザーが広告主／ASPへ確認し、
**18件すべて終了**と判明した。

- したがって現在の稼働8件が実態と一致しており、案件マスタの設定変更は不要。
- **今後この18件について「新しいURLを探す」作業はしない。** 復活するときは新規案件として扱う。
- リンクが開かない＝終了、という当初の目印は結果的に正しかったが、**判定手法としては
  「終了」と「貼り替え」を区別できない**点は変わらない。次に同じ調査をするときも、
  直近まで成果が出ていた案件は広告主に確認してから結論を出すこと。

### 代理店名は「小中様」で確定

会議記録の「小永（コナカ）」は誤りで、設定タブの表記どおり **小中様** が正しい。
代理店マスタの登録（`01kn` / 小中様 / 停止）は正しいので変更不要。

### 代理店別サマリーを実装した（件数・承認率・稼働状況）

「代理店ごとの件数・稼働率の数値化」に対応。`代理店別サマリー` シート（生成物）。

- **申請状況一覧と同じ元データ（`collectApplications_`）から集計する。** 数え方を二重に持つと
  2つの表が食い違うため。承認判定も既存の `getAdvertiserApprovalFlags` を再利用する。
- 区分別ブロック: 申請数 / 承認 / トラッキング漏れ / 未判定 / 承認率 / 直近30日 / 初回申請 / 直近申請。
  **承認率は 承認÷申請数**で、未判定を別列に出して解釈できるようにした。
  **「稼働率」は直近30日の申請数と直近申請日で表す**（動いている代理店が一目で分かる）。
- **申請ゼロの代理店も行として出す。** 出さないと「登録したのに動いていない」代理店が
  表から消えて見えなくなる。
- 案件×区分の申請数ブロックも併記し、合計の多い順に並べる。
- 日次トリガーは `rebuildAgencyReports`（申請状況一覧＋サマリーをまとめて再生成）へ差し替えた。
  旧 `buildApplicationStatusSheet` 直登録のトリガーは `ensureAppStatusTrigger` が見つけ次第削除する。

### ASPの管理画面調査は認証情報をチャットに出さない方法で行う

ユーザーがASPのログイン情報を入手した。**認証情報をチャットへ貼らない**方針は変えない
（会話ログに残るため）。ユーザーの実Chromeへ `chrome-live` で接続して画面を見る経路を使う。
手順は CLAUDE.md の Browser Control 節（`chrome://inspect/#remote-debugging` を一時的にON、
作業後にOFFへ戻す）。

## 2026-08-20: ASP（FPR）の管理画面を調査。承認の突合は自動化できるが、今のキーでは精度が足りない

ユーザーの実Chrome（`chrome-live`）経由で FPR のパートナー管理画面を読み取り専用で調査した。
**認証情報はチャットにも本ファイルにも残さない。**

### 分かった構造

- パートナーID: 株式会社市場作り（ID 3）。メニューは 時系列レポート / サイト広告別レポート /
  **獲得ログ** / 報酬金額一覧。
- **獲得ログ（`/partneradmin/report/action/log/list`）が1件ごとのデータを持つ。** 列は
  `承認日時 / クリック日時 / 注文日時 / 広告ID / 広告名 / サイト名 / OS / リファラ / 報酬額 /
  ステータス / セッションID / CL付加情報1〜5`。
- **顧客の氏名は出ない**（会議での認識どおり）。ただし**クリック日時が出る**のが重要。
- **CSVを機械で取得できる。** `/partneradmin/report/action/log/list/csv` へ POST。パラメータは
  `searchReportDateType`（1=クリック日時 / 2=注文日時 / 3=承認日時）、`searchReportDate`
  （`2026年04月01日 - 2026年08月20日` 形式）、`searchRows`。**CSRFトークンは無い。**
  2026-04-01〜08-20 で451件を1リクエストで取得できた。**文字コードは UTF-8**（当初 Shift_JIS と誤って記録していたので訂正）。
- ステータスは `承認 / 否認 / 承認待ち` の3値（451件の内訳: 承認316 / 否認15 / 承認待ち120）。
- サイト名は450件が「柳沢様」、1件が「小中様」。**サイト名は担当者の区別には使えない。**
- **ログイン維持はセッションCookie。GASからは直接叩けない**（Cookie認証のため）。
  自動化するならブラウザ経由（PC上のPlaywright等）か、月1回の手動ダウンロード＋取り込み。

### 突合の精度を実データで測った（ここが判断の核心）

自社シートは**クリック日時を記録している**（`buildRow` の `clickAt`）。これをキーに451件と
突き合わせた結果:

- **一致は 217/451 = 48%（±120秒）。** ただし**一致するときは中央値0秒**で、111件が完全一致、
  183件が±2秒以内。**キーとしては正しいが、半分が拾えない。**
- 拾えない理由は2つ考えられる。(1)クリック日時は**利用者の端末時計**で記録しているためズレる
  （未一致の最寄り差は262〜5619秒とバラバラ）(2)成果は出たが自社フォームに提出されていない。
- **この精度で承認を自動で書き換えるのは危険。** 誤った行に承認を付ける事故が起きる。

### 解決策: CL付加情報に自社の申請IDを流す

獲得ログに **CL付加情報1〜5** の欄があり、**現在は全件空**。アフィリエイトURLに独自の値を
付けて渡すと、そのまま獲得ログへ返る仕組み。ここに自社側の一意なIDを流せば**突合は100%**になる。

- クエリパラメータ名は広告ごとのリンクタグに埋め込まれている（`広告選択` の詳細で
  `makeItemTag(bannerNum, itemUrlColumn)` として生成される）。**実際の名前は広告のリンクタグを
  1件表示すれば分かる。** 推測で決め打ちしないこと。
- 実装側は、フォームのアフィリエイトボタンに `&<param>=<申請ID>` を付けるだけでよい。
  申請IDは受信時に採番して回答行へ持たせる。

### 今の時点で見つかった実害: 8件・77,000円

突合できた217件でステータスを比べたところ、食い違いが実在した。

- **ASP=承認 なのに自社シート=トラッキング漏れ（❌）: 8件・報酬合計 77,000円。**
  差は0〜2秒・案件名も一致しており、同一の成果とみて間違いない。
  **自社側で請求対象から外しているが、ASPでは承認・支払い対象になっている。**
- ASP=承認待ち / 自社=承認: 5件（自社が先に承認を付けている。確定待ち）
- ASP=承認 / 自社=未記入: 3件、ASP=否認 / 自社=未記入: 2件
- ASP=承認 / 自社=承認: 153件（大多数は一致している）

**再現手順**: 獲得ログCSVを取得し、クリック日時±120秒で自社シートの回答行と突き合わせ、
ASPの「承認」と自社の承認列（`getAdvertiserApprovalFlags`）を比較する。

## 2026-08-20: ASP突合をGASに実装した。時刻だけで突合すると誤マッチが出るので案件名を条件に加えた

「まず8件を直す → 突合を仕組みにする」を1つの道具にまとめた。毎月同じ照合が回せる。

### 作ったもの

- `ASP獲得ログ` シート: ASPからダウンロードしたCSVを取り込む場所。
- `reconcileAspLog()`: クリック日時で突き合わせ、`ASP突合結果` シートへ判定を出す。**書き戻さない。**
- `applyAspReconciliation()`: 「適用」にチェックの付いた**要修正**行だけ、自社の承認欄へ ⭕ を書く。

### 書き戻す方向を片側だけにした

**「ASPは承認・自社は未承認」→ ⭕ を書く、の一方向だけ**にした。逆（自社が承認・ASPが承認待ち/否認）
で承認を消すのは業務判断であり、機械が消してよいものではないため報告に留める。

### 時刻だけで突合してはいけない（実データで踏んだ）

移植版で検証したところ、**2026-06-18 のリビンマッチの成果が、案件の違う
「HOME4U家づくりのとびら」の行に68秒差で誤マッチした。** そのまま適用していれば別案件の行へ
承認を書き込んでいた。

対策として `aspNameMatches_()` を入れ、**許容範囲内の候補のうち案件名が対応するものを優先**し、
対応する候補が無い場合は「要確認: 時刻は近いが案件名が対応しない」として**適用対象から外す**。
ASP側は `【CTIA様専用】…` `…（特別単価）` のような修飾が付くので、`【】` `（）` `()` を落として
前方後方の包含で比べる（イエイ ↔ 不動産一括査定イエイ、ポケットリサーチ（特別単価）↔ ポケットリサーチ）。

### 検証（GAS実装をPythonへ移植して実データで実行）

- ガード追加前: 要修正11件（うち1件が上記の誤マッチ）。
- **ガード追加後: 要修正10件・報酬合計97,000円**（すべて案件名一致・差0〜2秒）。要確認33件・
  一致174件・未一致234件。
- 要確認が5→33件に増えたのは、案件名が対応しない組をそちらへ回したため。**正しい挙動。**
- 当初「8件・77,000円」と報告したが、**移植して数え直したところ10件・97,000円**だった
  （ポケットリサーチ（特別単価）とスマモニ（特別単価）の2件は自社が「未記入」で、
  最初の集計では「トラッキング漏れ」だけを見ていたため漏れていた）。

### 運用

ASPはセッションCookieでログインするため**GASからCSVを取得できない**。ユーザーがASPの獲得ログから
CSVをダウンロード（または `chrome-live` で取得）し、`ASP獲得ログ` シートへ取り込む。
**CSVは UTF-8**（当初 Shift_JIS と記録したのは誤り）。

## 2026-08-20: 請求漏れ10件の承認修正を用意。請求書は番号の再利用が見つかり保留

### 承認の修正（適用待ち）

突合で確定した10件を直す `applyAspFix20260820()` を用意した（メニュー「請求漏れ10件を承認へ直す」）。
**行番号だけを信じて書かない。** 書く前に (1)その行のクリック日時がASPの記録と120秒以内か
(2)いま承認になっていないか を検証し、外れたらスキップして理由を返す。下見（dryRun）も用意した。

### 請求書は機械的に出し直せない状態だった

請求ブックを最新版で取得して突き合わせたところ、**請求増額は ASP報酬合計の97,000円ではなく
87,000円**だった。差の10,000円は既に請求済みだったため。判定は「ASPの承認件数」と
「請求書の数量」を案件ごとに比べて行った（自社シートの承認欄だけでは判定できない）。

**手を止めた理由が2つある。ブックは変更していない。**

1. **7月分の請求書が現在のブックに存在しない。** 2026-08-07 のローカル控えでは 202600012 が
   アフィリンク7月分だったが、**最新版では同じ番号が別の請求先へ再利用されている**。
   8/7 に作った内容が Drive へ保存されないまま番号が使われた形。どの番号で出し直すかは
   採番の判断なので勝手に決めない。
2. **6月分のアフィリンク請求書が見当たらない。** 6月は自社シートで88件・ASPで83件の承認があるのに、
   対応する請求書が無い。6月分の修正1件の帰属先を決められない。

### 請求書の作られ方について分かったこと（次に作る人向け）

- **単価はASPの報酬額と一致する。** 従来「単価はユーザーに聞く」としていたが、獲得ログの報酬額を
  見れば分かる。
- **7月分は自社シートの承認件数と完全に一致していた。** ただし1案件だけ承認欄が空のまま請求されており、
  ここはシートから機械的には出てこない（別途の判断が入る）。
- **5月以前の請求書は自社シートの承認件数と一致せず、ASP側の件数とおおむね合う。**
  作成根拠がシートではなくASPだった可能性が高い。**「シートの承認＝請求」と決めつけないこと。**

金額・請求先を含む詳細は `02_設計/請求突合_20260820.md`（**public リポジトリのため gitignore 済み**）。

## 2026-08-20: 代理店登録が56秒かかり「送れない」に見えた。原因は設定シートの全行スキャン

### 起きたこと

代理店登録フォームで「送れない」エラーが出たと報告があった。**実際にはサーバー側は成功しており**、
代理店1件が正しく登録され、取扱案件8件のチェックとリンク集URLも作られていた。
実行ログでは `doPost` が **55.5秒 と 56.2秒 で2回「完了」**していた（2回目は利用者の再送）。
メールアドレスで冪等にしてあるため重複行は生まれていない。

**画面がエラーに見えたのは、応答が返るまでが長すぎたため。**

### 原因: 1枚のシートを全行読む関数を、31枚 × 3回 呼んでいた

`getFormCodeFromSheet` / `getAgencyCode` / 表示名の取得は、いずれも
`sheet.getDataRange().getValues()` を使う。これは**回答データを含む全行×全列**を読む。
`listCaseSheets_` はこの3つをシートごとに呼んでいたので、設定シート31枚に対して
フルスキャンが約90回。さらに `listActiveCases_` と `syncAgencyCaseMatrix` から
繰り返し呼ばれ、登録1回で数百回のシート読み取りになっていた。

**設定はシートの先頭8行にしかない。** そこだけ読めばよかった。

### 直した内容と実測

1. `listCaseSheets_` を**先頭14行×2列の1回読み**に変え、実行中はキャッシュする。
2. `listActiveCases_` を**案件マスタの1回読み**に変えた。案件マスタは案件コードと案件名の
   両方を持つので、設定シートを開く必要がそもそも無い。
3. 代理店の申請件数は、案件シートを走査せず**申請状況一覧を1回読む**方式にした。
4. `applyCaseVisibility` も同じ理由で狭い読み取りに変えた。

実測（代理店が開くリンク集）: **15.7〜24.4秒 → 3.4〜4.1秒**。
申請フォーム（代理店リンク）は 4.9〜5.5秒で、紹介者の自動設定も正しい。

### 副作用への対処

件数を日次生成の `申請状況一覧` から引くようにしたため、そのままだと**代理店が申請しても
翌朝までリンク集の件数が0のまま**になる。初めて使う代理店に「自分の申請が数えられていない」と
見えるので、`doPost` から `申請状況一覧` の2行目へ即時挿入するようにした（新しい順のため先頭）。
色は次の日次生成で整う。

### 教訓

- **GASで「シート枚数 × 全行スキャン」をやると簡単に分単位になる。** 既存のヘルパーが
  `getDataRange()` を使っていないか、ループの中で呼ぶ前に確認する。
- **集約シート（案件マスタ・申請状況一覧）を読み取り用のモデルとして使う。** 元データを
  毎回集計し直さない。
- **公開画面から呼ばれる経路は実測する。** 動くことと使えることは別。
